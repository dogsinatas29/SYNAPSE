# 👻 SYNAPSE: Ghost Nodes (고스트 노드) 가이드

고스트 노드(Ghost Node)는 **소스 코드 상에서 `import`나 `require` 등으로 참조하고 있지만, 프로젝트 폴더 내에 실제 물리적인 파일이 존재하지 않는 외부 모듈이나 라이브러리**를 시각화하기 위한 특수한 노드입니다.

## 🌟 최신 동작 규칙 (v0.3.17 기준)

현행 시스템은 복잡한 의존성 그래프의 가독성을 유지하고 실질적인 아키텍처에 집중하기 위해 고스트 노드를 다음과 같이 제어합니다:

### 1. 스마트 필터링 및 블랙리스트 (Smart Filtering)
과도한 시각적 노이즈를 막기 위해, 시스템 내장 모듈이나 표준 라이브러리는 시각화 대상에서 제외됩니다.
- **제외 대상**: `os`, `fs`, `path`, `sys`, `vscode`, `react`, `unittest` 등 주요 빌트인/표준 패키지.
- **커스터마이징**: `IGNORE_GHOST_TARGETS` 설정을 통해 프로젝트별로 특정 모듈을 필터링할 수 있습니다.

### 2. 고스트 정화 (Ghost Purification - v0.3.14 도입)
단순한 외부 라이브러리가 아닌 **문서화 파일(.md)**이나 아키텍처 문서는 더 이상 고스트 노드로 캔버스를 어지럽히지 않습니다.
- 모든 확장자가 `.md`인 고스트 참조는 자동으로 **Documentation Shelf (문서 보관함)**로 격리되어 관리됩니다.

### 3. 격자 주권 및 겹침 방지 (Grid Sovereignty)
고스트 노드 역시 시스템의 전역 격자 주권을 따릅니다.
- **40px 그리드 스냅**: 모든 고스트 노드는 40px 격자에 맞춰 정렬됩니다.
- **Overlap Resolver**: 고립되거나 겹치는 고스트 노드는 다른 노드들과 충돌하지 않도록 자동으로 위치가 보정됩니다.

### 4. 자동 확정 및 검증 (Auto-Validation)
코드 분석을 통해 발견된 고스트 노드 연결은 시스템에 의해 즉시 확정 상태로 표시됩니다.
- 별도의 사용자 승인 절차 없이 실시간으로 의존성 그래프에 반영됩니다.

### 5. 시각적 형태 (Visual Representation)
물리적 파일이 아님을 즉각적으로 인지할 수 있도록 다음과 같이 렌더링됩니다:
- **테두리**: 점선 (`dash: [4, 4]`)
- **투명도**: 반투명 (`opacity: 0.6` 수준 유지)
- **컬러**: 아키텍처 중립적인 **그레이 컬러 (`#928374`)**로 표시됩니다. (과거 블루 컬러에서 변경됨)
- **라벨**: 복잡한 기호 없이 순수 모듈/경로 이름으로 표시되어 가독성을 높였습니다.

---
> **Note**: 고스트 노드는 아키텍처의 "누락된 링크"를 찾는 데 유용합니다. 만약 고스트 노드가 실제 구현되어야 할 파일이라면, 해당 노드를 통해 아키텍처의 실체화를 계획할 수 있습니다.
