# v0.3.11_Bootstrap.md (HARD LOCK - COMPLETE)

---

## 0. SYSTEM DIRECTIVE

This document defines the ONLY valid execution order.
ALL .md documents in the workspace MUST be loaded.
No omission allowed.

---

## 1. LOAD RULE (STRICT)

### Rule:

- Load ALL `.md` files in the directory
- Sort by filename (ascending, lexicographical)
- Load sequentially

```text
v0.3.xx_*.md → sorted → sequential load
```

---

## 2. REQUIRED CORE FILES (MUST EXIST)

The following documents are mandatory:

- v0.3.11_DraftContract.md
- v0.3.11_NodeEdgeDraft.md
- v0.3.12_ExecutionLayer.md
- v0.3.11_Architecture_Set.md
- v0.3.11_VerificationLayer.md

IF ANY MISSING → STOP

---

## 3. AUTO-INCLUDE RULE

Any additional `.md` file:

- MUST be loaded
- MUST NOT be ignored
- MUST NOT be conditionally skipped

---

## 4. STAGE LOCK SYSTEM

Execution stages are strictly enforced.

---

## STAGE 1 — Draft Initialization

- Construct DraftGraph

LOCK:
- nodes exist
- edges valid

---

## STAGE 2 — Pre Validation

validateDraft(graph)

LOCK:
- no invalid references
- no circular dependency

---

## STAGE 3 — Commit Planning

planCommit(graph)

LOCK:
- ChangeSet exists

---

## STAGE 4 — Execution (Transaction)

executeWithTransaction(graph)

LOCK:
- all files written
- no partial writes

FAIL → rollback

---

## STAGE 5 — Verification

verifyCommit(result)

LOCK:
- files exist
- imports valid
- build success

FAIL → rollback

---

## 5. GLOBAL HARD RULES

- NEVER skip any .md file
- NEVER skip validation
- NEVER skip verification
- NEVER mutate DraftGraph after validation
- NEVER partially commit

---

## 6. FAILURE PROTOCOL

ANY failure:

1. rollbackTransaction()
2. STOP execution

---

## 7. SUCCESS CONDITION

ALL stages pass → valid system state

---

## 8. SINGLE ENTRY POINT

```ts
function run(graph: DraftGraph) {
  validateDraft(graph)
  planCommit(graph)
  executeWithTransaction(graph)
  verifyCommit()
}
```

---

## 9. FINAL DIRECTIVE

> ALL documents define the system.
> Missing even one invalidates correctness.
