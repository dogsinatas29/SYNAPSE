# v0.3.11 Core Freeze - 변경 지침

## 1. GraphModel.ts
### 유지
- Node / Edge / Cluster 구조

### 제거
- addNode()
- addEdge()
- addCluster()

### 결과
- 읽기 전용 구조

---

## 2. DataPipeline.ts
### 제거
- canvasEngine.dispatch('ADD_NODE', ...)
- canvasEngine.dispatch('CONNECT_EDGE', ...)

### 변경
- nodes.push(...)
- edges.push(...)

### 추가
- extractGraphElements()

### 결과
- 순수 데이터 변환기

---

## 3. StateManager.ts
### 제거
- mutateAddNode()
- mutateConnectEdge()

### 유지
- UI 상태 관리

### 결과
- View 상태만 관리

---

## 4. BootstrapEngine.ts
### 변경
scan → buildGraph → stateManager 전달

---

## 5. graphBuilder.ts (신규)
- buildGraph()
- freezeGraph()

---

## 6. 핵심 규칙
- Graph immutable
- dispatch 기반 생성 제거
- UI와 완전 분리

---

## 7. 다음 단계

Core Freeze 이후 단계는:

### 목표
책임 분리 및 단순화

### 수행 작업
- Renderer에서 graph 접근 제거
- Projection Layer 도입
- Rule Engine 분리
- 이벤트 → 상태 변경 구조 전환

### 핵심 방향
- 데이터 / 표현 / 규칙 완전 분리
- 단일 흐름 유지

---

## 결론
Core Freeze 이후 단계는
'책임이 뒤섞인 구조를 분리하고 단순화하는 과정'이 맞다.
