
# v0.3.1_Bootstrap_Locked.md (UPDATED)

---

## LOAD ORDER (Execution Binding)

1. v0.3.1_Data_Pipeline.md
2. v0.3.1_Graph_Model.md

3. v0.3.1_State_Snapshot_System.md

4. v0.3.1_Event_Loop_Control.md
5. v0.3.1_Performance_Budget.md

6. v0.3.1_Grid_System_Architecture.md

7. v0.3.1_Warning_Rendering_Optimization.md

8. v0.3.1_Debugging_Trigger_Architecture.md

9. v0.3.1_System_Validation.md   ← NEW (MANDATORY)

10. v0.3.1_Iron_Grid_Refined.md

---

## 목적

단계 실패 시 다음 단계 진입을 강제 차단한다.

또한 Validation을 통해 시스템의 정상 동작을 검증한다.

---

## GLOBAL RULE

```
IF (current_phase != PASS)
    BLOCK(next_phase)
    HALT_EXECUTION
```

---

## VALIDATION LOCK

```
IF (validation != PASS)
    BLOCK(FINAL)
    REQUIRE_FIX
```

---

## Phase 1 — Data + Graph

조건:
- Node 생성 성공
- Edge 연결 정상
- include 파싱 완료

FAIL:
- Graph 생성 중단
- Snapshot 생성 금지

---

## Phase 2 — Snapshot

조건:
- snapshot 저장 성공
- snapshot restore 성공

FAIL:
- Event Loop 진입 금지

---

## Phase 3 — Control

조건:
- infinite loop 없음
- tick 안정성 확보

FAIL:
- Grid 생성 금지

---

## Phase 4 — Grid

조건:
- deterministic 좌표
- snapping 정확

FAIL:
- Rendering 금지

---

## Phase 5 — Rendering

조건:
- frame drop 없음
- warning render 정상

FAIL:
- Debug 기능 금지

---

## Phase 6 — Debug

조건:
- 클릭 분석 정상
- 병목 탐지 가능

FAIL:
- Iron Grid 진입 금지

---

## Phase 7 — Validation (NEW)

조건:
- 모든 테스트 통과
- 실패 케이스 정상 차단 확인

FAIL:
- FINAL 진입 금지
- SYSTEM LOCK 유지

---

## 📅 작업 정보 - **상태:** ✅ Completed
- **관련 마일스톤:** v0.3.0 (이전 버전 링크) 
- **목표:** Phase-based 초기화 강제 및 시스템 안정성 검증 통과

## 📝 Post-Work Log (작업 후 기록) 
- ✅ PhaseManager 구현 (src/core/PhaseManager.ts): 7단계 상태 관리 및 강제 순서 보장.
- ✅ Phase 2: 로컬 파일 기반 Snapshot Persistence 및 Restore 구현 (src/core/SnapshotSystem.ts).
- ✅ Phase 3: Sovereign Control 및 16ms 주기 이벤트 루프 안정화 (src/core/ControlSystem.ts).
- ✅ Phase 4: 40px Snap-to-Grid 및 소스/문서 영역 분리 레이아웃 (src/core/GridSystem.ts).
- ✅ Phase 5: Dynamic Threshold 기반 Render Budget 및 엣지 필터링 (src/core/RendererCore.ts).
- ✅ Phase 6: Diagnostic Overlay (Phase Status, Budget, Ticks) 시각화 (src/core/DebuggerSystem.ts).
- ✅ Phase 7: System Validation 검증 (src/test/SystemValidation.test.ts) 통과 및 Webpack 빌드 성공.
- ✅ [HOTFIX] CanvasPanel.sendProjectState Phase Violation 해결: Phase 1, 2 자동 동기화 로직 추가로 초기 로딩 락(Lock) 현상 제거.
- ✅ v0.3.1 릴리즈 노트 작성 및 VSIX 빌드 준비 완료.
- ✅ [UI Lockdown] Context Vault UI 전체 차단:
    - ArchitectureExplorer (사이드바)에서 Context Vault 항목 제거.
    - CanvasPanel (웹뷰)에서 Context Vault 클러스터(ctx_vault_cluster) 주입 코드 비활성화.
    - .synapse_contexts 디렉터리 자동 생성 로직 및 관련 명령(synapse.flushLogs, synapse.manualPbScan) 비활성화.
- ✅ [UX Enhancement] Status Overlay Draggable 구현:
    - Phase Debug Overlay (#phase-overlay)에 mouse event 활성화 및 cursor: move 적용.
    - makeDraggable 헬퍼 함수 강화로 absolute/right/bottom 좌표 자동 전환 지원.
    - Status window(#phase-overlay) 및 Inference window(#dtr-controller) 이동 기능 추가.
- ✅ [Visual Convention] Node Status & High Contrast Badge 정규화:
    - Necrosis(💀) 및 Tombstone(🪦) 아이콘 렌더링 규약 적용.
    - 엣지 상태 배지 이모지화 (❓ Pending, ❗️ Confirmed) 및 ❌ 삭제 배지 추가.
    - Loop 엣지 점선([1, 3] Dotted) 스타일 정밀 차별화.
- ✅ [Performance] Render Budget Threshold 상향:
    - SOFT_BUDGET_NODES를 80에서 200으로 상향하여 중대형 프로젝트 가시성 확보.
    - DTR 가중치 로직 반전으로 고복잡도(High Importance) 노드 필터링 방지.
- ✅ [Final UI Lockdown] Context Vault 관련 진입점 전면 숨김:
    - 웹뷰 상단 툴바(Main Menu)의 "⏺ Context" 드롭다운 메뉴 숨김.
    - 웹뷰 "View" 드롭다운 메뉴 내 "📂 Context Vault Panel" 버튼 숨김.
    - Context Vault 관련 모든 UI 버튼을 사용자 가시 영역에서 제거하여 접근 차단 완료.
- ✅ [Visual Convention Alignment] WebGL(3D) 가속 모드 규약 동기화:
    - 🎨 [Node Status] Active, Ghost, Deleted 색상 및 테두리 스타일(Dashed) 동기화.
    - 🎨 [Status Icons] Necrosis(💀), Tombstone(🪦) 아이콘 및 노이즈 효과 구현.
    - 🎨 [Glow/Pulse] High DTR(보라색 글로우), Warning(적색 펄스) 실시간 애니메이션 추가.
    - 🔗 [Edge Styles] Dependency, Data Flow 등 타입별 색상/두께 및 점선 스타일 적용.
    - 🔗 [Marching Ants] 점선 엣지(API Call, Loop)에 이동 애니메이션 적용.
    - 🏷️ [Interactive Badges] ❌(Delete), ❓(Pending), ❗️(Confirmed) 배지 3D 모드 지원.
