# 🚀 Implementation Plan - Milestone v0.2.32

## 🎯 Goal
- **[CORRUPTION]** Prevent LOB Sniffer from collecting binary fragments (encoding fixes).
- **[VOLUME]** Prevent abnormal session file growth (guarding against 80k+ line runaway).
- **[STABILITY]** Strengthen real-time deduplication logic in ForensicAdapter.

## 🏗️ Technical Architecture

### 1. Heuristic Quality Filter
- **Target:** `ChatExtractor.ts`, `VscdbAdapter.ts`
- **Logic:** Implement `isLikelyHumanReadable` function to calculate text quality scores and filter binary orphans.

### 2. Stateful Deduplication
- **Target:** Metadata persistence
- **Logic:** Persist `seenHashes` into `.synapse_contexts/.seen_hashes.json` to maintain deduplication state across extension restarts.

### 3. Append Throttle & Limit
- **Target:** `PromptLogger.ts`
- **Logic:** Set a 5MB threshold for session files and automatically halt collection if exceeded.

### 4. PB Sniffer Optimization (Cooling)
- **Target:** `fs.watch` usage
- **Logic:** Implement debounce for file watching to reduce CPU overhead during heavy chat sessions.

---

## ✅ TODO List

- [x] **[FIX]** Apply readability filter to `ChatExtractor` and `VscdbAdapter`.
- [x] **[OPT]** Implement `seenHashes` persistence logic.
- [x] **[GUARD]** Add session file size limit and user notifications in `PromptLogger`.
- [x] **[BUILD]** Generate and verify `synapse-visual-architecture-0.2.32.vsix`.

---

## 📈 Status: ✅ Completed
- The features have been implemented and validated as per the milestone document.
- Speakers "You" and "Bard" (Gemini) are now correctly identified in `ClipboardScraperAdapter`.
- Binary plain-text sniffing (>7 chars) is optimized in `PbSessionWatcher`.
