# Implementation Plan - Milestone v0.2.42 (The Island Finder)

## 📅 Status: ✅ Completed / 🏗️ Verification
- **Goal**: Implement "Island Finder" algorithm to solve UTF-16LE encoding parsing issues in `.pb` files.
- **Success Criteria**: Successful extraction of chat logs from binary files with >5% Hangeul density.

## 🛠️ Tasks & TODO List

### 1. Core Logic (Island Finder)
- [x] Refactor `DirectPbExtractor.ts` to support multi-encoding scanning.
- [x] Implement `findIslands` method:
    - [x] Scan `utf16le` with offset 0.
    - [x] Scan `utf16le` with offset 1 (staggered).
    - [x] Scan `utf8` as fallback.
- [x] Implement `isLegitConversation` heuristic:
    - [x] Hangeul density check (> 5%).
    - [x] English sentence check (space count > 3).
    - [x] Length check (> 25 characters).
    - [x] Noise filtering (Paths, Hex, SQL, Workbench prefixes).
- [x] Implement `detectRole` logic:
    - [x] Detect `user` markers (문항, 해줘, 알려줘, fix, ?, etc.).
    - [x] Use length heuristic (< 800 chars for User).

### 2. Integration & Package
- [x] Update `package.json` to `0.2.42`.
- [x] Package the extension as `synapse-visual-architecture-v0.2.42.vsix`.
- [x] Ensure `PromptLogger` correctly handles the `system_msg` from the miner.

### 3. Documentation
- [x] Create/Update `mile_stone/v0.2.42.md`.
- [x] Create `release_note/v0.2.42_release_notes.md`.

## 🧪 Verification Plan (Extermination Test Plan)
1. **Manual VSIX Install**: Install `v0.2.42.vsix`.
2. **Trigger Conversation**: Ask "안녕? (UTF-8/UTF-16 혼용 테스트) 지금 이 코드의 인코딩 상태가 어떤지 fix해줄 수 있어? 보여줘."
3. **Log Audit**: Check `.synapse_contexts/session_*.md` for:
    - `system_msg: PB Miner: Extracted N messages using Island Finder.`
    - Correct User/Assistant attribution.
    - Clean text without binary noise.
