# encoding: utf-8
# 🚀 Implementation Plan - Milestone v0.2.45 "The Wildcard"

## 📅 작업 정보 - **상태:** 🏗️ Planned
- **버전:** v0.2.45
- **목표:** 특정 필터링 없이 `antigravity.*` 네임스페이스의 모든 명령어를 요격하여 '골든 키' 데이터를 추출하고 영구 박제함.

---

## 🛠️ TODO 리스트

### 1. CommandInterceptor.ts 필터링 로직 수정 (Wildcard)
- [ ] `patchExecuteCommand`의 `if` 조건에서 `antigravity.` 접두사의 모든 명령어를 수용하도록 확장.
- [ ] 인자에 대한 '정밀 타격' 없이 일단 1차적으로 모든 인자를 직렬화하여 덤프 시도.

### 2. Deep Argument Inspector (재귀 탐색기) 구현
- [ ] `findMessageInArgs`를 재귀 함수 형태로 리팩토링.
- [ ] `Depth 5`까지 모든 객체 및 배열 내부의 문자열을 수집.
- [ ] 수집된 문자열 중 한글(Hangeul) 정규 표현식(`/[가-힣]/`)에 매칭되는 경우 'GOLDEN_DATA' 플래그 추가.

### 3. Blackbox Logger (intercept_hits.log) 정교화
- [ ] 발견된 문자열뿐만 아니라 해당 문자열이 어떤 명령어(ID)에서 발생했는지 명확하게 상호 참조되도록 기록.
- [ ] `[${timestamp}] HIT [${commandId}] -> Content: ${content}` 형식 유지.

### 4. 시동 테스트 및 초기 덤프 확인
- [ ] `Developer: Reload Window` 실행 후 상태바 아이콘 확인.
- [ ] 채팅창에 "시냅스, 지금 대화 들리나?" 입력 후 `intercept_hits.log` 실시간 모니터링.

---

## 🧠 상세 구현 로직 (Recursive Scan)
```typescript
private recursiveScan(obj: any, depth = 0): string[] {
    if (depth > 5 || !obj) return [];
    const results: string[] = [];
    
    if (typeof obj === 'string' && obj.trim().length > 1) {
        results.push(obj.trim());
    } else if (Array.isArray(obj)) {
        for (const item of obj) {
            results.push(...this.recursiveScan(item, depth + 1));
        }
    } else if (typeof obj === 'object') {
        for (const key in obj) {
            results.push(...this.recursiveScan(obj[key], depth + 1));
        }
    }
    return results;
}
```

---

## 📝 릴리즈 노트 계획 (Expected)
- **v0.2.45**: "The Wildcard" - 모든 `antigravity.*` 명령어 전수 조사 및 실시간 한국어 페이로드 추출 성공.
