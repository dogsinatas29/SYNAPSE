# Implementation Plan - v0.3.14 Emergency Fix

## 1. Problem Analysis
- **UI Crash**: `Uncaught SyntaxError: Identifier 'cpX' has already been declared` at `ui/canvas-engine.js:6691`. This is caused by redundant `const` declarations for `cpX` and `cpY` within the `renderEdge` function.
- **Backend Error**: `TypeError: Cannot assign to read only property 'label' of object '#<Object>'` in `GraphModel.ts`. This occurs when updating common system clusters (e.g., `cluster_ghosts`) that might be frozen in the state.

## 2. Proposed Changes

### A. UI: `ui/canvas-engine.js`
- Remove lines 6691 and 6692 which redeclare `cpX` and `cpY`.
- These variables are already defined at lines 6611 and 6612.

### B. Core: `src/core/GraphModel.ts`
- Modify the `loadFrom` method to update system clusters without mutating frozen objects.
- Instead of direct property assignment, update the clusters array with cloned objects.

## 3. TODO List
- [x] Create Milestone v0.3.14
- [ ] Fix `ui/canvas-engine.js` redeclarations.
- [ ] Fix `src/core/GraphModel.ts` property mutation.
- [ ] Verify fix by checking VSCode console and screen rendering.
