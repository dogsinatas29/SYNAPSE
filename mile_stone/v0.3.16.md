# encoding: utf-8
# 🚀 Milestone v0.3.16-alpha - Minimalist Logic View

## 📅 작업 정보
- **상태:** ✅ Completed
- **관련 마일스톤:** v0.3.15
- **목표:** "안 보이면 겹치지도 않는다." - 겹침 문제를 알고리즘이 아닌 '사용자의 필요'에 따른 온오프 제어로 해결.

## 🧠 상세 설계 및 로직
- Logic: `if (edgeState === 'HIDDEN') return null;` (단, 선택된 노드의 연결 엣지는 반투명 예외 처리)
- Focus: 복잡한 라우팅 알고리즘(직각, 길찾기 등)을 전면 배제하고, 사용자 제어권에 기반한 'Visibility Layer' 구현.
- Goal: "안 보이면 겹치지도 않는다." - 겹침 문제를 알고리즘이 아닌 '사용자의 필요'에 따른 온오프 제어로 해결.
- 구현 방법 : View menu에 엣지 숨김 / 뱃지만 숨김 모드 작업
    - 엣지 숨김 모드 : 모든 엣지와 엣지와 연관된 벳지를 숨김. 화면에 노드와 클러스터만 보이게 작업. **(단, 노드 선택 시 해당 노드에서 나가는 엣지는 반투명하게 노출시켜 연결성을 확인할 수 있도록 함)**
    - 뱃지만 숨김 모드 : 엣지위에 나오는 벳지만 숨김

## 🛠️ 기술적 변경 사항
- **Node Update:** N/A (상태 유지를 위해 노드 간 연결 속성은 유지 집중)
- **Edge Update:** WebGL 렌더링 또는 Canvas 렌더링 시 노드 간 Edge Visibility 상태에 따른 렌더링 스킵(Return null) 논리 추가. 필요 시 `RenderProtocol`을 사용해 제어권 통합.
- **File Changes:** `ui/index.html` 내 View 메뉴 추가, `ui/canvas-engine.js` 및 `ui/webgl-renderer.js`에서 렌더 플래그 적용, 상태 전파 로직 구현.

## ⚠️ 예외 처리 및 주의 사항
- 엣지를 숨겼을 때도 노드 간의 논리적 연결(Data Structure)은 유지되어야 함.
- 토글 스위치 조작 시 렌더링 부하(Jank)가 발생하지 않도록 O(1) 렌더링 분기 적용(제미나이 퍼포먼스 룰 준수). 반복 계산 없이 상태 변경에 의존한 GPU/CPU Workload를 설계할 것.
- 엣지 및 라인 컨벤션 규칙이 정상적으로 적용되는지 확인
  - **Dependency:** #ebdbb2 solid 2px (Standard module dependency or import)
  - **Data Flow:** #83a598 solid 3px (Heavy data transfer or payload movement)
  - **Event:** #fe8019 solid 2px (Event triggers or asynchronous callbacks)
  - **Conditional:** #d3869b solid 1px (Conditional branches like if/else or match)
  - **Origin:** #d65d0e solid 1.5px (Prompt origin links for AI logic tracking)
  - **API Call:** #8ec07c dashed 2px (External API or cross-service network calls)
  - **DB Query:** #d3869b solid 3px (Database queries, mutations, or transactions)
  - **Loop / Back:** #fe8019 dotted 2px (Loop-backs (while/for) or reverse logic flow)
  - **Highlighted:** #fabd2f pulse 5px (Active execution path (Hovered/Selected))

## 📝 Post-Work Log (작업 후 기록)
- `ui/index.html`에 Edge Visibility 메뉴 및 토글 버튼 삽입 완료.
- `ui/canvas-engine.js`에 글로벌 상수 `window.edgeVisibilityMode` 및 EventListener 바인딩. 2D 엔진 fallback 최적화 적용 확인.
- `ui/webgl-renderer.js` 프래그먼트 셰이더 패치 작업. Edge Rendering Loop 내 O(1) 조기 반환(`continue`), 선택된 노드의 엣지 예외 반투명 처리(alpha 0.3) 할당 검증.
- **뱃지 중복 출력(Doubled Icons) 해결:** 줌 레벨에 따라 화살표 아이콘(Midpoint)과 통합 배지(Large Capsule) 레이어를 분리하여 중복 출력을 원천 차단.
- **가시성 모드 정밀화:** `NO_BADGES` 모드에서 화살표 내 텍스트 아이콘 및 검증 배지가 모두 숨겨지도록 O(1) 조건문 강화.
- 릴리즈 노트 `v0.3.16_release_notes.md` 저장 완료.
