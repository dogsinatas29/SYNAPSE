# encoding: utf-8
# 🚀 Milestone v0.3.30 - Collaborative Harvest System

## 📅 작업 정보
- **상태:** ✅ MVP Complete — v0.3.30 Collaborative Harvest System (106/106 ✅)
- **관련 명세:** `mile_stone/SYNAPSE_v0.3.30_BOOTSTRAP_SPECIFICATION.md`
- **목표:**
  - SYNAPSE를 단독 시각화 도구에서 **Submission 기반 협업 Harvest 시스템**으로 전환한다.
  - 역할(Role) 기반 권한 체계를 도입한다 (Lead = Review/Verify/Harvest, Member = Work/Submit).
  - Submission Snapshot을 영구 보존 기록으로 관리한다.
  - REST Transport로 제출을 전송하고 Remote Layer로 시각화한다.
  - Architecture Index → Reference Verification → Harvest 로 이어지는 완전한 파이프라인을 구축한다.

## 🧠 Phase Architecture

```
Phase 1 — Project Boundary Foundation ✅
Phase 2 — Identity & Session Foundation ✅
Phase 3 — REST Collaboration Transport / Submission Boundary ✅
Phase 4 — Remote Layer Visualization ✅
Phase 5 — Architecture Index Generation ✅
Phase 6 — Reference Verification Layer ✅
Phase 7 — Harvest Engine ✅
Phase 8 — Boundary Enforcement ✅
Phase 9 — Acceptance Test ✅
```

---

## 📋 Phase 1 Technical Details

### ProjectMetadata Path Validation Logic
```
Input: targetPath (e.g., "../etc/passwd" or "src/main.ts")
  1. resolved = path.resolve(projectRoot, targetPath)
  2. root = path.resolve(projectRoot)
  3. Check: resolved.startsWith(root + path.sep) || resolved === root
  4. Fail → throw Error / return false
  5. Pass → allow operation
```

### SymbolIndex Data Model
```
SymbolIndex
  ├─ projectName: string
  ├─ projectRoot: string
  ├─ folderTree: FolderTree (recursive)
  │    ├─ path: string
  │    ├─ name: string
  │    ├─ children: FolderTree[]
  │    └─ fileCount: number
  ├─ fileRegistry: Map<relPath, FileEntry>
  │    ├─ filePath, fileName, extension, size
  │    ├─ isSource, isDocumentation
  │    └─ functionCount, classCount
  └─ functionCatalog: FunctionEntry[]
       ├─ functionId (base64-encoded unique)
       ├─ functionName
       ├─ filePath
       ├─ className (nullable)
       └─ lineNumber
```

### SnapshotSystem Versioning
```
Save Flow:
  graphModel.createSnapshot()
    → snapshot.snapshotVersion = increment
    → snapshot.metadata = { projectUUID, projectName, snapshotCount }
    → snapshot.checksum = sha256(JSON.stringify(data)).slice(0,16)
    → history.push({ id, timestamp, version, checksum, data })
    → persist to data/synapse_history.json (max 20)

Restore Flow:
  → find snapshot (by ID or latest)
  → if checksum exists: recompute and compare (warn on mismatch)
  → graphModel.restoreSnapshot(target)
```

### Source File Detection 확장자 목록
| Category | Extensions |
|----------|-----------|
| Source | .py .ts .js .rs .cpp .c .go .java .kt .swift |
| Documentation | .md .mdx .rst .txt |
| Excluded | .md .txt .log .report (Phase 5 Function Catalog에서) |

### 파일 요약
| File | Lines | Type |
|------|-------|------|
| `src/core/ProjectMetadata.ts` | 104 | 신규 |
| `src/core/SymbolIndex.ts` | 159 | 신규 |
| `src/test/phase1_validation.test.ts` | 165 | 신규 |
| `src/types/schema.ts` | +17 | 수정 |
| `src/core/SnapshotSystem.ts` | +26 | 수정 |
| `src/core/DataPipeline.ts` | +30 | 수정 |
| `src/extension.ts` | +10 | 수정 |

---

## 📜 Bootstrap Specification Overview

`mile_stone/SYNAPSE_v0.3.30_BOOTSTRAP_SPECIFICATION.md` — 508 lines

### Development Constitution (9 Rules)
| Rule | Principle |
|------|-----------|
| 1. Think Before Coding | 질문 → 접근법 제시 → 트레이드오프 → 단순 해법 우선 |
| 2. Simplicity First | 불필요한 기능/추상화/최적화 금지 |
| 3. Minimal Changes | 검증된 시스템 재작성 금지, 기존 아키텍처 보존 |
| 4. Goal-Oriented Execution | Goal → Plan → Implementation → Validation (단계 생략 금지) |
| 5. No Hallucinated APIs | 존재하지 않는 API/함수/라이브러리 발명 금지 |
| 6. Stable Code Protection | 검증된 코드는 보호됨, 명시적 요청 없이 수정 금지 |
| 7. Context Confirmation | 수정 전 context/files/dependencies 확인, 추정 구현 금지 |
| 8. Rendering Isolation | Graph/File/Flow View 전환 시 렌더링 상태 완전 초기화 |
| 9. Approval Gate | Read Spec → Plan → Request Approval → Receive Approval → Implement |

### Core Principles
| Principle | 정의 |
|-----------|------|
| Connect = Submission | 연결은 협업 시작이 아니라 작업 완료 후 제출 |
| Member = Work + Submit | 멤버는 작업하고 제출 |
| Lead = Review + Verify + Remote Edit + Harvest | 리드는 검토/검증/원격수정/수확 |
| Submission Snapshot = Permanent Record | 제출 스냅샷은 영구 보존, 변경 불가 |
| Layer = Derived Projection | 레이어는 계산된 분류, 저장된 값은 캐시일 뿐 |
| No Database Principle | 제출 스냅샷이 유일한 진실 공급원, DB 불필요 |

### Complete Workflow
```
Client Work Complete → Submission Snapshot → Remote Layer Projection →
Architecture Index → Architect Review → Remote Edit (Optional) →
Verification → Harvest → Master Layer Update
```

---

## ✅ Phase 1 — Project Boundary Foundation

### Mission
협업 기능(Session, Layer, Verify, Harvest)의 기반이 되는 프로젝트 경계를 확립.
모든 후속 시스템은 Phase 1에서 정의된 Project Boundary를 반드시 준수.

### Goal
1. 프로젝트 간 접근 불가능
2. 프로젝트 외부 파일 접근 불가능
3. 프로젝트 상태를 Snapshot으로 저장 가능
4. 프로젝트의 Symbol Index 생성 가능
5. Server가 Project Metadata의 단일 관리자

### Created Files

#### `src/core/ProjectMetadata.ts`
- **Singleton** — Server(Extension)가 단일 소유
- `data/project_metadata.json`에 저장
- Fields: projectUUID, projectName, version, createdAt, updatedAt, snapshotCount, metadataVersion
- `validatePath(targetPath)` — 프로젝트 경계 검증 (path.resolve → startsWith root)
- `initialize(projectRoot, projectName?)` / `loadSync()` / `saveSync()`
- Node fs 사용, vscode 의존성 없음

#### `src/core/SymbolIndex.ts`
- **Singleton** — 프로젝트 단일 Symbol Registry
- **Components:** FolderTree (계층형 디렉토리), FileEntry (파일 메타데이터), FunctionEntry (함수 카탈로그)
- `rebuildFromFiles(files[])` — 전체 인덱스 재구축
- `addFunction()` — 함수 등록
- 소스 확장자: .py .ts .js .rs .cpp .c .go .java .kt .swift
- 문서 확장자: .md .mdx .rst .txt

### Modified Files

#### `src/types/schema.ts`
- `ProjectMetadata` interface 추가
- `GraphSnapshot` 확장: `snapshotVersion`, `metadata`, `checksum`

#### `src/core/SnapshotSystem.ts`
- Versioned save (snapshotVersion 자동 증가)
- SHA-256 기반 checksum 무결성 검증
- Metadata stamp (ProjectMetadata UUID/Name/SnapshotCount 포함)
- History 최대 20개, 각 entry에 version/checksum 필드

#### `src/core/DataPipeline.ts`
- Security boundary: `processFiles()` 진입 시 모든 파일 경계 검증
- SymbolIndex integration: rebuildFromFiles + addFunction

#### `src/extension.ts`
- ProjectMetadata / SymbolIndex 초기화 코드 추가

### Test File
`src/test/phase1_validation.test.ts` — 10개 테스트 전부 통과 ✅

| # | Test | Result |
|---|------|--------|
| 1 | ProjectMetadata 생성 및 로드 | ✅ |
| 2 | ProjectMetadata 저장 및 재로드 | ✅ |
| 3 | Security: 경계 내 경로 허용 | ✅ |
| 4 | Security: 경계 외부 경로 차단 | ✅ |
| 5 | SymbolIndex 초기화 및 파일 등록 | ✅ |
| 6 | SymbolIndex FileRegistry 확장자 분류 | ✅ |
| 7 | SymbolIndex FunctionCatalog | ✅ |
| 8 | SymbolIndex FolderTree | ✅ |
| 9 | ProjectMetadata 단일 관리자 (Singleton) | ✅ |
| 10 | SymbolIndex 단일 관리자 (Singleton) | ✅ |

### Exit Criteria
| Criteria | Status |
|----------|--------|
| 프로젝트 간 접근 불가능 | ✅ `validatePath()` enforces workspace root |
| 프로젝트 외부 파일 접근 불가능 | ✅ DataPipeline + ProjectMetadata dual enforcement |
| 프로젝트 상태를 Snapshot으로 저장 가능 | ✅ Versioned snapshot with checksum |
| 프로젝트의 Symbol Index 생성 가능 | ✅ FolderTree + FileRegistry + FunctionCatalog |
| Server가 Project Metadata의 단일 관리자 | ✅ Singleton, persisted to JSON |
| TypeScript Compilation | ✅ `tsc --noEmit` clean |
| Webpack Build | ✅ `npm run compile` pass |

---

## ✅ Phase 2 — Identity & Session Foundation

### Mission
프로젝트 참여자와 권한을 관리하고 협업 세션을 생성하기 위한 기반 시스템 구축.
네트워크 전송(REST/HTTP)은 Phase 3에서 별도로 다룸.

### Core Principle
권한은 사용자 기준이 아닌 **Project + Role** 기준으로 계산.

### Created Files

#### `src/core/collaboration/IdentityManager.ts`
- **Identity** 생성 (Server만 가능, Client 임의 생성 불가)
  - `userId`: `usr_{timestamp}_{random}` 포맷
  - `displayName`: 사람이 읽을 수 있는 이름
- **Role** 할당 (Lead / Member)
  - 동일 사용자가 프로젝트마다 다른 Role 가능
- **Permission** 체계 (Role에서 파생):
  - **Lead:** CreateSession, CloseSession, ApproveJoin, Verify, Harvest, RemoteEdit, DisconnectClient, ReadLayer, WriteLayer
  - **Member:** JoinSession, LeaveSession, SubmitChanges, ReceiveCommands, ReadLayer
- `getProjectMembers(projectUUID)` — 프로젝트 멤버 조회
- 프로젝트 간 권한 완전 격리

#### `src/core/collaboration/SessionManager.ts`
- **Session Lifecycle:** Created → Open → Active → Closing → Closed
- **Session Registry:** sessionId, projectUUID, leadId, state, members, maxMembers(10)
- **Join Workflow:** Join Request → Validation → (Lead Approval implied) → Member
- **Leave Workflow:** Member can leave, Lead must close
- **Close Flow:** closing → disconnect all → closed
- **Security Constraints:**
  - Closed session 참가 금지
  - Role 없는 사용자 참가 금지
  - Capacity(10) 초과 금지
  - Lead는 leave 불가 (close만 가능)

#### `src/core/collaboration/RuntimeInitializer.ts`
- **Runtime State Machine:** stopped → initializing → ready → error
- **Startup Flow:**
  1. Load Project Metadata
  2. Load Symbol Index
  3. Initialize Identity Manager
  4. Initialize Session Manager
- `assertReady()` — 게이트 함수, runtime 미준비 시 throw

### Modified Files

#### `src/extension.ts`
- Phase 2 초기화: `RuntimeInitializer.getInstance().initialize()` 추가

### Test File
`src/test/phase2_validation.test.ts` — 14개 테스트 전부 통과 ✅

| # | Test | Result |
|---|------|--------|
| 1 | Identity 생성 | ✅ |
| 2 | Role 할당 | ✅ |
| 3 | Role 없는 Identity 할당 실패 | ✅ |
| 4 | Lead Permissions | ✅ |
| 5 | Member Permissions | ✅ |
| 6 | Runtime Startup Flow | ✅ |
| 7 | Session 생성 (Lead only) | ✅ |
| 8 | Session 생성 실패 (Member) | ✅ |
| 9 | Join / Leave Workflow | ✅ |
| 10 | Session Close | ✅ |
| 11 | Closed Session 참가 실패 | ✅ |
| 12 | Role 없는 사용자 참가 실패 | ✅ |
| 13 | ProjectMembers 조회 | ✅ |
| 14 | Security: 프로젝트 간 권한 격리 | ✅ |

### Exit Criteria
| Criteria | Status |
|----------|--------|
| Identity System Complete | ✅ |
| Role System Complete | ✅ |
| Permission System Complete | ✅ |
| Runtime Initialization Complete | ✅ |
| Session Lifecycle Complete | ✅ |
| Session Registry Complete | ✅ |
| Join / Leave Workflow Complete | ✅ |
| 10 Client Support (capacity) | ✅ |
| Role 없는 사용자 참가 실패 | ✅ |
| Closed Session 참가 실패 | ✅ |
| TypeScript Compilation | ✅ `tsc --noEmit` clean |
| Phase 3 (REST Transport) 진행 가능 | ✅ |

---

---

## ✅ Phase 3 — REST Collaboration Transport / Submission Boundary

### Mission
클라이언트의 완료된 작업 공간이 Review Artifact가 되는 경계를 정의.
Submission Snapshot은 Runtime Snapshot(GraphSnapshot/ViewSnapshot/CanvasState/ProjectState)과 명확히 구분됨.

### Core Principles
```
Connect        = Submission
SubmissionSnapshot = Review Baseline
Member         = No Write Authority After Submission
Lead           = Remote Edit Authority During Review
Server         = Submission Owner
```

### Created Files

#### `src/core/collaboration/SubmissionManager.ts`
- **SubmissionSnapshot** — 협업 전용 artifact, runtime snapshot과 완전 분리
  - `id`, `projectUUID`, `sessionId`, `clientId`, `files[]`, `timestamp`, `immutable`
  - 생성 조건: active session + member role + project boundary validation
- **ReviewState** — pending → review → approved/rejected
  - `startReview()` — Lead가 review 시작
  - `approveSubmission()` / `rejectSubmission()` — 검토 완료
- **Member Frozen** — 제출 후 member write 권한 자동 박탈
  - `isMemberFrozen(clientId)` — 상태 확인
  - `unfreezeMember(clientId)` — 수동 해제
- **Remote Edit** — Lead가 submitted workspace의 파일을 수정
  - `applyRemoteEdit()` → `RemoteEditAction` 로그 보존
  - Original content는 action log에 백업
  - Snapshot 자체는 불변 개념 유지, content는 workspace 차원에서 수정
- **경계 검증:**
  - Only member can submit (lead can't)
  - Only active session allows submission
  - All files validated against ProjectMetadata.validatePath()
  - Verification/Harvest 수행 금지 (Phase boundary)

### Modified Files

#### `src/types/schema.ts`
- `SubmissionFile`, `SubmissionSnapshot`, `ReviewState`, `ReviewStateType`, `RemoteEditAction` interfaces 추가

### Test File
`src/test/phase3_validation.test.ts` — 12개 테스트 전부 통과 ✅

| # | Test | Result |
|---|------|--------|
| 1 | Submission Snapshot Created | ✅ |
| 2 | Submission Snapshot Stored | ✅ |
| 3 | Member Write Authority Revoked (Frozen) | ✅ |
| 4 | Lead Remote Edit Available | ✅ |
| 5 | Snapshot Accessible For Review | ✅ |
| 6 | Original Snapshot Remains Immutable | ✅ |
| 7 | Lead Workspace Edit Preserves Original | ✅ |
| 8 | Verification Not Executed (Phase boundary) | ✅ |
| 9 | Harvest Not Executed (Phase boundary) | ✅ |
| 10 | Review approval and rejection flow | ✅ |
| 11 | Member cannot submit outside active session | ✅ |
| 12 | Non-member cannot submit | ✅ |

### Exit Criteria

| Criteria | Status |
|----------|--------|
| Submission Snapshot Created | ✅ |
| Submission Snapshot Stored | ✅ |
| Review State Established | ✅ |
| Member Frozen | ✅ |
| Lead Review Authority Available | ✅ |
| Remote Edit Available | ✅ |
| Original Snapshot Preserved | ✅ |
| No Analysis Performed | ✅ |
| No Verification Performed | ✅ |
| No Harvest Performed | ✅ |
| TypeScript Compilation | ✅ `tsc --noEmit` clean |
| Phase 4 (Remote Layer Visualization) 진행 가능 | ✅ |

---

## ✅ Phase 4 — Remote Layer Visualization

### Mission
제출된 Artifact(SubmissionSnapshot)를 Runtime 구조(GraphSnapshot)로 변환하고 Layer를 분류하여 가시성 모델을 생성.

### Core Principles
```
Layer ≠ Canvas / Composition Stack / Render Surface
Layer = Visibility Classification Group
Layer contains: Nodes + Clusters (Edges have no layer)
Layer is recomputed every load (stored values are cache only)
No Database (all structures rebuildable from SubmissionSnapshot)
```

### Created Files

#### `src/core/collaboration/RemoteLayerProjector.ts`
- **Stateless Projector** — SubmissionSnapshot → ProjectionResult
- **Node Classification Rules:**
  | Pattern | Layer |
  |---------|-------|
  | hasAtomicSignature | user |
  | node_manual_* | user |
  | documentation (.md etc.) | user |
  | user cluster member | user |
  | external://* | external |
  | ghost://* | external |
  | otherwise | ai |
- **Cluster Classification Rules:**
  | Pattern | Layer |
  |---------|-------|
  | cluster_ghosts | external |
  | folder_* | ai |
  | sys_* | ai |
  | other | user |
- **System Clusters:** cluster_ghosts, sys_cluster_reserved, doc_shelf (always present)
- **Visibility Model:** showBaseLayer, showUserLayer, showExternalLayer (all=true by default)
- **Edges have no layer** — visibility = Visible(From) AND Visible(To)
- **Stateless:** No persistence, no database, full rebuild from snapshot

> **Implementation Note (v0.3.30)**: The current layer model uses three functional categories (`ai`/`user`/`external`). Per-client layers (e.g. "Client Layer A" / "Client Layer B") are not implemented. The `Master Layer / Client Layer` separation described in the bootstrap specification is a target architecture for future iterations.

### Test File
`src/test/phase4_validation.test.ts` — 14개 테스트 전부 통과 ✅

| # | Test | Result |
|---|------|--------|
| 1 | Submission Snapshot Imported | ✅ |
| 2 | Runtime Projection Complete | ✅ |
| 3 | Node Layer — AI | ✅ |
| 4 | Node Layer — User (docs) | ✅ |
| 5 | Node Layer — External (ghosts) | ✅ |
| 6 | Cluster Layer Classification | ✅ |
| 7 | System Clusters Always Present | ✅ |
| 8 | Visibility Controls | ✅ |
| 9 | GraphSnapshot correct layers | ✅ |
| 10 | Folder-based cluster grouping | ✅ |
| 11 | Ghost nodes in cluster_ghosts | ✅ |
| 12 | Edges have no layer | ✅ |
| 13 | No Database used | ✅ |
| 14 | Layer for source files | ✅ |

### Exit Criteria

| Criteria | Status |
|----------|--------|
| Submission Snapshot Imported | ✅ |
| Runtime Projection Complete | ✅ |
| Nodes Classified | ✅ |
| Clusters Classified | ✅ |
| Visibility Operational | ✅ |
| Remote Layer Visible | ✅ |
| No Database Used | ✅ |
| TypeScript Compilation | ✅ `tsc --noEmit` clean |
| Phase 5 (Architecture Index Generation) 진행 가능 | ✅ |

---

## ✅ Phase 5 — Architecture Index Generation

### Mission
SubmissionSnapshot에서 Verification 준비 구조(Architecture Index)를 추출.

```
Submission Snapshot
        ↓
Architecture Projection
        ↓
Architecture Index
          ├─ Project Tree
          ├─ Folder Tree
          ├─ Source File Registry
          └─ Function Catalog
```

### Core Principles
- **Submission Snapshot is the only authoritative source.**
- All Phase5 outputs are derived artifacts (rebuildable any time).
- If inconsistency: Submission Snapshot wins.
- **No Database** — Architecture Index is a transient derived structure.
- Phase5 does NOT perform verification, pass/fail decisions, harvest, layer assignment, or visualization.

### Created File
`src/core/collaboration/ArchitectureIndexBuilder.ts`

| Component | Description |
|-----------|-------------|
| Project Tree | Single root project node with UUID |
| Folder Tree | Physical directory hierarchy from submitted files |
| Source File Registry | `.py .ts .js .rs .cpp .c .go .java .kt .swift` only |
| Function Catalog | Top-level functions + class methods (excludes lambdas, closures) |

### Exclusions (not in Architecture Index)

| Excluded | Reason |
|----------|--------|
| `.md .txt .log .report` | Documentation/reports not verification structure |
| `external://` `ghost://` files | Not part of submitted project |
| Layer Information | Layer = derived projection, not authoritative |
| User Clusters | = UI State, not reconstructable from snapshot |
| External Ghosts | = Visualization Artifact |
| Visualization Metadata | Node positions, canvas layout, view state, etc. |
| Runtime Snapshots | GraphSnapshot, ViewSnapshot, CanvasState |

### Function Extraction (regex-based, per language)

| Language | Pattern |
|----------|---------|
| Python | `def name(` — class methods detected via indentation |
| TypeScript/JS | `function name(`, `method(` inside class |
| Rust | `fn name(` |
| Go | `func name(` / `func (r T) name(` |
| Java/Kotlin | `name(params) {` (filtered against keywords) |
| C/C++ | `name(params) {` |
| Swift | `func name(` |

### Test File
`src/test/phase5_validation.test.ts` — 14개 테스트 전부 통과 ✅

| # | Test | Result |
|---|------|--------|
| 1 | ArchitectureIndex created from SubmissionSnapshot | ✅ |
| 2 | Project Tree contains project node | ✅ |
| 3 | Folder Tree hierarchy | ✅ |
| 4 | Source File Registry (source only, no docs/ghosts) | ✅ |
| 5 | Language detection from extension | ✅ |
| 6 | Top-level function extraction | ✅ |
| 7 | Class methods included | ✅ |
| 8 | Function ID unique and deterministic | ✅ |
| 9 | No Database used | ✅ |
| 10 | No layer information | ✅ |
| 11 | No visualization metadata | ✅ |
| 12 | No user clusters / external ghosts | ✅ |
| 13 | Source file entry correct structure | ✅ |
| 14 | Function entry correct structure | ✅ |

### Exit Criteria

| Criteria | Status |
|----------|--------|
| Submission Snapshot Parsed | ✅ |
| Project Tree Generated | ✅ |
| Folder Tree Generated | ✅ |
| Source File Registry Generated | ✅ |
| Function Catalog Generated | ✅ |
| No Layer / Viz / Ghost / Cluster info included | ✅ |
| TypeScript Compilation | ✅ `tsc --noEmit` clean |
| Phase 6 (Reference Verification Layer) 진행 가능 | ✅ |

---

## ✅ Phase 6 — Reference Verification Layer

### Mission
Architecture Index → Reference Discovery → Graph Construction → Verification → Harvest Candidate Set.

```
Architecture Index
        ↓
Reference Discovery (import/include/use/require scanning)
        ↓
Reference Resolution
    ├─ Resolved → Edge Generation
    └─ Unresolved → Ghost Projection (external:// / ghost://)
        ↓
Reference Graph (File Nodes + Folder Clusters + Edges + Ghosts)
        ↓
Verification Report (Findings + Stats)
        ↓
Architect Decision → Harvest Candidate Set
```

### Core Principles
- **Phase5 = What Exists, Phase6 = How Files Relate**
- File-level references only — NO function call analysis
- Ghost generation ≠ Failure (ghosts are architectural findings)
- No database (transient, rebuildable from snapshot)
- Edges categories: INCLUDE, REFERENCE, CALL, DB_QUERY, DATA_FLOW, EVENT, CONDITIONAL, LOOP_BACK

### Created File
`src/core/collaboration/ReferenceVerifier.ts`

### Reference Discovery (per language)

| Language | Patterns |
|----------|----------|
| Python | `import X`, `from X import Y` |
| TypeScript/JS | `import X from 'y'`, `require('y')` |
| Rust | `use crate::X`, `extern crate X`, `mod X` |
| C/C++ | `#include "X"`, `#include <X>` |
| Go | `import "X"` |
| Java | `import X.Y.Z` |
| Kotlin | `import X.Y.Z` |
| Swift | `import X` |

### Reference Resolution

| Scenario | Result |
|----------|--------|
| Direct file match | Edge to matched file |
| Extension match (`import auth` → `auth.py`) | Edge to matched file |
| Module path match (`import src.core` → `src/core.py`) | Edge to matched file |
| Known external package (axios, requests, serde, etc.) | `external://package` ghost |
| Unresolved internal reference | `ghost://reference` ghost |

### Verification Report

| Stat | Description |
|------|-------------|
| totalFiles | Source files in Architecture Index |
| totalEdges | All discovered reference edges |
| totalGhosts | External + Internal ghost nodes |
| resolvedReferences | References resolved to source files |
| unresolvedReferences | References that became ghosts |
| disconnectedFiles | Files with no incoming/outgoing edges |

### Findings

| Finding | Severity | Meaning |
|---------|----------|---------|
| EXTERNAL_DEPENDENCY | info | Reference resolved to known external package |
| GHOST_PROJECTION | warning | Internal reference could not be resolved |
| DISCONNECTED_COMPONENT | info | File has no references to/from other files |

### Harvest Candidate Set

| Field | Description |
|-------|-------------|
| decision | Architect decision (accept/modify/resubmit/prepare_harvest) |
| candidates | Array of HarvestCandidateEntry (file + edges) |
| report | Full VerificationReport |

### Test File
`src/test/phase6_validation.test.ts` — 14개 테스트 전부 통과 ✅

| # | Test | Result |
|---|------|--------|
| 1 | Reference Discovery scans source files | ✅ |
| 2 | Resolved references generate edges | ✅ |
| 3 | Unresolved references create ghost projections | ✅ |
| 4 | External dependencies detected | ✅ |
| 5 | Internal unresolved references become ghost:// | ✅ |
| 6 | Graph constructed with all components | ✅ |
| 7 | Verification report contains findings | ✅ |
| 8 | Disconnected files detected | ✅ |
| 9 | No function call analysis | ✅ |
| 10 | Edge categories are valid | ✅ |
| 11 | Ghost nodes track referencing files | ✅ |
| 12 | Harvest Candidate Set generated | ✅ |
| 13 | Candidate entries have correct structure | ✅ |
| 14 | No database used | ✅ |

### Exit Criteria

| Criteria | Status |
|----------|--------|
| Reference Discovery Completed | ✅ |
| Reference Resolution Completed | ✅ |
| Edges Generated | ✅ |
| Ghosts Projected | ✅ |
| Reference Graph Constructed | ✅ |
| Verification Executed | ✅ |
| Verification Report Produced | ✅ |
| Harvest Candidate Set Generated | ✅ |
| TypeScript Compilation | ✅ `tsc --noEmit` clean |
| Phase 7 (Harvest Engine) 진행 가능 | ✅ |

---

## ✅ Phase 7 — Harvest Engine

### Mission
Approved Workspace → Master Layer 물리적 Materialization.

```
Submission Snapshot
        ↓
Review + Remote Edit
        ↓
Approved Workspace
        ↓
Harvest (Physical Copy)
        ↓
Master Layer (.synapse/master/)
```

### Core Principles
- **Harvest = Collection** — No analysis, no verification, no conflict resolution
- **Files copied exactly as approved** — No rename, no normalization, no reinterpretation
- **Original snapshot preserved** — Immutable, never modified by Harvest
- **Folders created exactly as approved** — Case-sensitive, no normalizing
- **Graph is advisory; Harvest is physical**
- **All review decisions must occur before Harvest**
- **Session eligible for disconnect after successful Harvest**

### Created File
`src/core/collaboration/HarvestEngine.ts`

### Input/Output

| Input | Description |
|-------|-------------|
| approvedFiles | SubmissionFile[] (post-remote-edit) |
| originalSnapshot | Original SubmissionSnapshot (preserved) |
| verificationReport | From Phase 6 (advisory) |

| Output | Description |
|--------|-------------|
| HarvestResult | filesHarvested, foldersCreated, masterLayerPath |
| Master Layer | `{projectRoot}/.synapse/master/` — files written to disk |
| Snapshot Archive | `{projectRoot}/.synapse/snapshots/{id}/snapshot.json` |

### Architecture

```
HarvestEngine.harvest(HarvestInput) → HarvestResult
  ├─ Skip external:// ghost:// files
  ├─ mkdir -p for each file's directory
  ├─ writeFileSync (exact content, exact path)
  ├─ Preserve original snapshot (JSON to .synapse/snapshots/)
  └─ Return HarvestResult with stats
```

### Exclusions
- External/ghost files are NOT harvested (visualization artifacts only)
- No findings or verification in output
- No database persistence

### Test File
`src/test/phase7_validation.test.ts` — 10개 테스트 전부 통과 ✅

| # | Test | Result |
|---|------|--------|
| 1 | Approved Workspace Exists | ✅ |
| 2 | Folder Structure Copied | ✅ |
| 3 | Source Files Copied | ✅ |
| 4 | Configuration Files Copied | ✅ |
| 5 | Documentation Files Copied | ✅ |
| 6 | Original Snapshot Preserved | ✅ |
| 7 | Harvest Does Not Modify Snapshot | ✅ |
| 8 | Harvest Does Not Execute Verification | ✅ |
| 9 | Harvest Completes Materialization | ✅ |
| 10 | Client Session May Be Closed | ✅ |

### Exit Criteria

| Criteria | Status |
|----------|--------|
| Approved Workspace Exists | ✅ |
| Harvest Executed | ✅ |
| Folder Structures Materialized | ✅ |
| Files Materialized | ✅ |
| Master Layer Updated | ✅ |
| Original Snapshot Preserved | ✅ |
| Review Lifecycle Completed | ✅ |
| Client Session Eligible For Disconnect | ✅ |
| Project State Updated | ✅ |
| TypeScript Compilation | ✅ `tsc --noEmit` clean |
| Phase 8 (Boundary Enforcement) 진행 가능 | ✅ |

> **Implementation Note (v0.3.30)**: Harvest is currently accessible through internal code paths and automated tests only. UI controls, REST endpoints (`POST /api/collab/harvest`), and VSCode commands are not exposed in v0.3.30. The ✅ status above reflects test-level verification of the `HarvestEngine` itself.

---

## ✅ Phase 8 — Boundary Enforcement

### Mission
Ownership → Authority → Access 체계를 중앙에서 강제하는 Boundary Guard 구현. 모든 이전 Phase에서 정의된 경계를 실제로 차단/허용.

```
Ownership → Authority → Access

Phase1 Project Boundary  →  Project Isolation
Phase2 Session Boundary  →  Session Isolation
Phase3 Submission        →  Submission Ownership
Phase4 Remote Layer      →  (visualization boundary)
Phase5 Architecture      →  Workspace Path Boundary
Phase6 Review            →  Remote Edit Authorization
Phase7 Harvest           →  Harvest Authorization
```

### Core Principles
- **Ownership determines authority. Authority determines access.**
- Access outside an object's ownership boundary is denied.
- Phase8 introduces no new collaboration features — only enforcement.
- Temporary collaboration artifacts must not persist after session termination.

### Created File
`src/core/collaboration/BoundaryGuard.ts` — `BoundaryError` class + `BoundaryGuard` singleton

| Method | Purpose | Exception |
|--------|---------|-----------|
| `assertProjectAccess` | Client has role in project | BoundaryError |
| `assertSessionAccess` | Client is member of active session | BoundaryError |
| `assertWorkspacePath` | Path within project root, no traversal | BoundaryError |
| `assertSubmissionAccess` | Client owns or leads the submission | BoundaryError |
| `assertRemoteEditAuth` | Only Lead, only within submission files | BoundaryError |
| `assertHarvestAuth` | Only Lead may harvest | BoundaryError |
| `assertAuthenticated` | Identity exists + has project role | BoundaryError |
| `cleanupSessionCache` | Clear project data after session ends | void |

### Enforcement Rules

| Boundary | Rule |
|----------|------|
| Project Isolation | Member of proj_a cannot access proj_b |
| Session Isolation | Non-member cannot access session |
| Workspace Boundary | `../`, absolute paths, path traversal → blocked |
| Submission Ownership | Member can only access own submission |
| Remote Edit | Only Lead, only on files in submission |
| Harvest | Only Lead |
| Authentication | Unknown identity or no role → denied |
| Cache Cleanup | Submission/Review data cleared on session close |

### Test File
`src/test/phase8_validation.test.ts` — 8개 테스트 전부 통과 ✅

| # | Test | Result |
|---|------|--------|
| 1 | Cross Project Access Denied | ✅ |
| 2 | Unauthorized Session Access Denied | ✅ |
| 3 | Workspace Escape Attempt Denied | ✅ |
| 4 | Unauthorized Submission Access Denied | ✅ |
| 5 | Unauthorized Remote Edit Denied | ✅ |
| 6 | Unauthorized Harvest Denied | ✅ |
| 7 | Invalid Identity Denied | ✅ |
| 8 | Session Cache Cleanup Success | ✅ |

### Exit Criteria

| Criteria | Status |
|----------|--------|
| Project Isolation Verified | ✅ |
| Session Isolation Verified | ✅ |
| Workspace Boundary Enforced | ✅ |
| Submission Ownership Verified | ✅ |
| Remote Edit Authorization Verified | ✅ |
| Harvest Authorization Verified | ✅ |
| Authentication Verified | ✅ |
| Cache Cleanup Verified | ✅ |
| All Boundary Tests Pass | ✅ |
| TypeScript Compilation | ✅ `tsc --noEmit` clean |
| Phase 9 (Acceptance Test) 진행 가능 | ✅ |

---

## ✅ Phase 9 — Acceptance Test

### Mission
전체 Collaborative Harvest Pipeline End-to-End 검증. 새로운 기능 없음 — 모든 이전 Phase가 올바르게 연결되어 동작하는지 확인.

```
Phase2 Identity → Phase3 Submission → Phase4 Projection → Phase5 Index
  → Phase6 Verification → Phase7 Harvest → Phase8 Boundary
```

### Core Principles
- **Phase9 is not a feature phase — Phase9 is a validation phase.**
- Every major workflow must complete successfully.
- End-to-End: Project Open → Runtime Ready → Session → Join → Submit → Project → Review → Verify → Remote Edit → Harvest → Session Close.

### Created File
`src/test/phase9_acceptance.test.ts` — 통합 Acceptance Test Suite

### Acceptance Scenarios

| # | Scenario | What It Validates | Result |
|---|----------|-------------------|--------|
| 1 | Server Startup | RuntimeInitializer → ready state | ✅ |
| 2 | Session Creation | IdentityManager.createIdentity + SessionManager.createSession + open + join → active | ✅ |
| 3 | Submission Workflow | SubmissionManager.createSubmission + startReview + approveSubmission | ✅ |
| 4 | Remote Layer Projection | RemoteLayerProjector.project → nodes with layer classification | ✅ |
| 5 | Architecture Index | ArchitectureIndexBuilder.build → SourceFileRegistry + FunctionCatalog | ✅ |
| 6 | Review & Verification | ReferenceVerifier.verify → Edges + Ghosts + Findings | ✅ |
| 7 | Remote Edit | SubmissionManager.applyRemoteEdit + getCorrectedWorkspace | ✅ |
| 8 | Harvest | HarvestEngine.harvest → Master Layer files written | ✅ |
| 9 | Session Termination | BoundaryGuard.cleanupSessionCache + SessionManager.closeSession | ✅ |
| 10 | End-to-End Pipeline | All 9 phases collectively verified | ✅ |

### Pipeline Flow (validated)

```
Runtime Ready                                    [Scenario 1]
        ↓
Lead Creates Identity + Session                   [Scenario 2]
        ↓
Member Joins + Creates Submission                 [Scenario 2→3]
        ↓
Review Started + Approved                        [Scenario 3]
        ↓
Remote Layer Projection (AI/User/External)        [Scenario 4]
        ↓
Architecture Index (Structure + Functions)        [Scenario 5]
        ↓
Reference Analysis + Verification Report          [Scenario 6]
        ↓
Remote Edit (Lead corrects file)                 [Scenario 7]
        ↓
Harvest → Master Layer (.synapse/master/)         [Scenario 8]
        ↓
Cache Cleanup + Session Close                    [Scenario 9]
```

### Test File
`src/test/phase9_acceptance.test.ts` — 10개 시나리오 전부 통과 ✅

| # | Test | Result |
|---|------|--------|
| 1 | Server Startup — Runtime Initialization | ✅ |
| 2 | Session Creation — Lead creates active session | ✅ |
| 3 | Submission Workflow — Member submits, lead reviews | ✅ |
| 4 | Remote Layer Projection — Submitted workspace visible | ✅ |
| 5 | Architecture Index Generated | ✅ |
| 6 | Review and Verification — Report produced | ✅ |
| 7 | Remote Edit — Lead corrects file | ✅ |
| 8 | Harvest — Master Layer materialized | ✅ |
| 9 | Session Termination — Clean closure | ✅ |
| 10 | End-to-End Full Workflow Validation | ✅ |

### Exit Criteria

| Criteria | Status |
|----------|--------|
| Runtime Operational | ✅ |
| Session Operational | ✅ |
| Submission Operational | ✅ |
| Remote Layer Operational | ✅ |
| Architecture Index Operational | ✅ |
| Review & Verification Operational | ✅ |
| Remote Edit Operational | ✅ |
| Harvest Operational | ✅ |
| Boundary Enforcement Operational | ✅ |
| End-to-End Workflow Successful | ✅ |
| All Tests Pass (106/106) | ✅ |
| TypeScript Compilation | ✅ `tsc --noEmit` clean |
| **SYNAPSE v0.3.30 Collaborative Harvest MVP Complete** | ✅ |

---

## 📋 Phase 1 Technical Details

### IdentityManager Permission Matrix
```
Permission            Lead    Member
─────────────────────────────────────
CreateSession         ✅      ❌
CloseSession          ✅      ❌
ApproveJoin           ✅      ❌
Verify                ✅      ❌
Harvest               ✅      ❌
RemoteEdit            ✅      ❌
DisconnectClient      ✅      ❌
ReadLayer             ✅      ✅
WriteLayer            ✅      ❌
JoinSession           ❌      ✅
LeaveSession          ❌      ✅
SubmitChanges         ❌      ✅
ReceiveCommands       ❌      ✅
```

### Session State Machine
```
Created ──(Lead open)──► Open ──(Member join)──► Active ──(Lead close)──► Closing ──► Closed
                              ▲                      │
                              └──(Member leave)──────┘
```

### Validation Rules (Join Gate)
1. Session exists
2. Session state ≠ 'closed'
3. Session state ≠ 'created' (must be open+)
4. User not already a member
5. Member count < maxMembers (10)
6. User has JoinSession permission
7. Lead has ApproveJoin permission
8. Project initialized

### Singleton Architecture
```
ProjectMetadata ──► data/project_metadata.json
     │
     ▼
RuntimeInitializer ──► state machine (stopped→ready→error)
     │
     ├── IdentityManager ──► in-memory Identity/Role/Permission maps
     └── SessionManager  ──► in-memory Session registry
     
SymbolIndex ──► rebuildable from files (no persistence needed)
```

### 파일 요약
| File | Lines | Type |
|------|-------|------|
| `src/core/collaboration/IdentityManager.ts` | 141 | 신규 |
| `src/core/collaboration/SessionManager.ts` | 170 | 신규 |
| `src/core/collaboration/RuntimeInitializer.ts` | 80 | 신규 |
| `src/test/phase2_validation.test.ts` | 200 | 신규 |
| `src/extension.ts` | +3 | 수정 |

---

## 📋 Phase 3 Technical Details

### Submission Flow
```
[Member]
  Workspace Complete
       │
       ▼
  createSubmission(projectUUID, sessionId, clientId, files)
       │
       ├── Validate: member role + active session + file boundaries
       ├── Create SubmissionSnapshot (immutable = true)
       ├── Create ReviewState (state = 'pending')
       └── Freeze Member (isMemberFrozen = true)
       
[Lead]
       │
       ▼
  startReview(submissionId, leadId)
       │
       ├── Validate: lead has Verify permission
       └── ReviewState.state = 'review'
       
       │
       ▼
  applyRemoteEdit(submissionId, leadId, filePath, newContent)
       │
       ├── Validate: lead has RemoteEdit permission
       ├── Backup originalContent to RemoteEditAction log
       └── Update file content in snapshot.files[]
       
       │
       ▼
  approveSubmission(submissionId, leadId)
       │
       └── ReviewState.state = 'approved' → Ready for Phase 7 Harvest
       
  rejectSubmission(submissionId, leadId, reason)
       │
       └── ReviewState.state = 'rejected' → Return to client
```

### Snapshot Distinction Matrix
```
                    SubmissionSnapshot    GraphSnapshot    ViewSnapshot    CanvasState    ProjectState
 ────────────────────────────────────────────────────────────────────────────────────────────────────
 Type               Collaboration         Runtime          Runtime         Runtime         Runtime
 Owner              Server                Extension        Extension       Extension       Extension
 Purpose            Review Baseline       Graph State      Visual State    Canvas State    Full State
 Immutable          ✅ (conceptual)       ❌               ❌              ❌              ❌
 Persistence        In-memory             synapse_history  Not persisted   project_state   project_state
 Contains           Files + Content       Nodes/Edges      Camera/Scale    Layout          Everything
 Phase              Phase 3               Phase 2          Phase 4         Phase 4         Phase 4
```

### RemoteEditAction Audit Trail
```
RemoteEditAction {
    filePath:      "src/main.ts"
    originalContent: "const x = 1;\n"
    newContent:      "const y = 2;\n"
    editedBy:       "usr_...lead..."
    editedAt:       1234567890
}
```
- 모든 Remote Edit은 action log에 기록됨
- Original content는 보존되어 Audit/비교에 사용 가능
- Log는 submission 단위로 수집 (`getRemoteEdits(submissionId)`)

### Validation Rules (Submission Gate)
1. Client must have 'member' role
2. Session must be in 'active' state
3. All file paths validated against project boundary
4. Member must not already be frozen
5. Client cannot be the session lead (lead ≠ member for submission)

### 파일 요약
| File | Lines | Type |
|------|-------|------|
| `src/core/collaboration/SubmissionManager.ts` | 203 | 신규 |
| `src/test/phase3_validation.test.ts` | 196 | 신규 |
| `src/types/schema.ts` | +25 | 수정 |

### 전체 파일 요약 (Phase 1-3)

| File | Lines | Phase | Type |
|------|-------|-------|------|
| `src/core/ProjectMetadata.ts` | 104 | P1 | 신규 |
| `src/core/SymbolIndex.ts` | 159 | P1 | 신규 |
| `src/core/collaboration/IdentityManager.ts` | 141 | P2 | 신규 |
| `src/core/collaboration/SessionManager.ts` | 170 | P2 | 신규 |
| `src/core/collaboration/RuntimeInitializer.ts` | 80 | P2 | 신규 |
| `src/core/collaboration/SubmissionManager.ts` | 203 | P3 | 신규 |
| `src/core/collaboration/RemoteLayerProjector.ts` | 160 | P4 | 신규 |
| `src/core/collaboration/ArchitectureIndexBuilder.ts` | 178 | P5 | 신규 |
| `src/core/collaboration/ReferenceVerifier.ts` | 223 | P6 | 신규 |
| `src/core/collaboration/HarvestEngine.ts` | 88 | P7 | 신규 |
| `src/core/collaboration/BoundaryGuard.ts` | 118 | P8 | 신규 |
| `src/test/phase1_validation.test.ts` | 165 | P1 | 신규 |
| `src/test/phase2_validation.test.ts` | 200 | P2 | 신규 |
| `src/test/phase3_validation.test.ts` | 196 | P3 | 신규 |
| `src/test/phase4_validation.test.ts` | 178 | P4 | 신규 |
| `src/test/phase5_validation.test.ts` | 167 | P5 | 신규 |
| `src/test/phase6_validation.test.ts` | 171 | P6 | 신규 |
| `src/test/phase7_validation.test.ts` | 115 | P7 | 신규 |
| `src/test/phase8_validation.test.ts` | 112 | P8 | 신규 |
| `src/test/phase9_acceptance.test.ts` | 155 | P9 | 신규 |
| `src/types/schema.ts` | +42 | P1+P3+P4+P5+P6+P7+P8+P9 | 수정 |
| `src/core/SnapshotSystem.ts` | +26 | P1 | 수정 |
| `src/core/DataPipeline.ts` | +30 | P1 | 수정 |
| `src/extension.ts` | +13 | P1+P2 | 수정 |

---

## 🔧 Post-Work Log

### Phase 1 (2026-06-07)
- ProjectMetadata singleton: server-owned project boundary manager
- SymbolIndex singleton: cross-file registry with folder tree, file registry, function catalog
- DataPipeline security integration: path validation at file processing entry
- SnapshotSystem upgrade: versioning, checksum, metadata embedding
- Validation suite: 10/10 tests passing

### Phase 2 (2026-06-07)
- IdentityManager: Identity + Role + Permission system
- SessionManager: full session lifecycle + join/leave + security constraints
- RuntimeInitializer: 4-step startup flow → ready state
- 14 validation tests all passing

### Phase 3 (2026-06-07)
- SubmissionManager: SubmissionSnapshot + ReviewState + MemberFrozen + RemoteEdit
- Schema: Phase 3 types (SubmissionSnapshot, ReviewState, RemoteEditAction)
- 12 validation tests all passing

### Phase 4 (2026-06-07)
- RemoteLayerProjector: SubmissionSnapshot → ProjectionResult (GraphSnapshot + Layer classification)
- Layer engine: Node classification (hasAtomicSignature→user, node_manual→user, docs→user, external://→external, ghost://→external, else→ai)
- Layer engine: Cluster classification (cluster_ghosts→external, folder_*→ai, sys_*→ai, else→user)
- Visibility model with 3 layer controls (showBaseLayer, showUserLayer, showExternalLayer)
- 14 validation tests all passing

### Phase 5 (2026-06-07)
- ArchitectureIndexBuilder: SubmissionSnapshot → ArchitectureIndex (ProjectTree + FolderTree + SourceFileRegistry + FunctionCatalog)
- Source-only file registry: excludes .md .txt .log .report, external://, ghost://
- Multi-language function extraction (Python, TS, JS, Rust, Go, Java, Kotlin, C, C++, Swift)
- Function ID generation (base64-encoded, deterministic)
- Excludes layer info, user clusters, external ghosts, visualization metadata, runtime snapshots
- 14 validation tests all passing

### Phase 6 (2026-06-07)
- ReferenceVerifier: ArchitectureIndex → ReferenceGraph + VerificationReport + HarvestCandidateSet
- Multi-language reference discovery (Python, TS/JS, Rust, C/C++, Go, Java, Kotlin, Swift)
- Reference Resolution: direct match, extension match, module path match
- Ghost Projection: external:// (known packages) / ghost:// (unresolved internal)
- Edge categories: INCLUDE, REFERENCE, CALL, DB_QUERY, DATA_FLOW, EVENT, CONDITIONAL, LOOP_BACK
- Verification Report: findings (EXTERNAL_DEPENDENCY, GHOST_PROJECTION, DISCONNECTED_COMPONENT)
- Architect Decision model: accept / modify / resubmit / prepare_harvest
- Harvest Candidate Set generation
- No function call analysis (file-level references only)
- 14 validation tests all passing

### Phase 7 (2026-06-07)
- HarvestEngine: Approved Workspace → Master Layer physical materialization
- Files/folders copied exactly as approved (no rename, no normalization)
- Original snapshot preserved to `.synapse/snapshots/{id}/`
- Skips external:// and ghost:// files (visualization artifacts)
- No verification, no conflict resolution, no analysis
- Session closure eligibility after successful harvest
- 10 validation tests all passing

### Phase 8 (2026-06-07)
- BoundaryGuard: centralized security enforcement for all collaboration boundaries
- 8 boundary checks: Project isolation, Session isolation, Workspace path, Submission ownership, Remote Edit auth, Harvest auth, Authentication, Cache cleanup
- BoundaryError exception for all violations
- Cache cleanup on session termination (submission/review data cleared)
- 8 validation tests all passing (all denial + cleanup success)

### Phase 9 (2026-06-07) — MVP Complete 🎉
- End-to-End Acceptance Test validating all 9 phases together
- 10 scenarios: Runtime → Session → Join → Submit → Remote Layer → Index → Verify → Remote Edit → Harvest → Session Close
- Single pipeline test chaining IdentityManager + SessionManager + SubmissionManager + RemoteLayerProjector + ArchitectureIndexBuilder + ReferenceVerifier + HarvestEngine + BoundaryGuard
- 10 acceptance tests all passing (106 total, 9 suites)

---

## 🐛 Hotfix: Collaboration Server Start/Stop (2026-06-09)

### 문제
WebView의 "서버 관리" 패널에서 "서버 시작" 버튼을 눌러도 상태가 계속 "중지됨"으로 표시됨.

### 원인
`src/webview/CanvasPanel.ts`의 `handleStartServer()`에 두 가지 버그 존재:

1. **`serverStarted` 메시지 누락 (치명적)**
   - `cp.spawn()`으로 서버 프로세스를 실행한 후 WebView에 `serverStarted` 커맨드를 전송하는 코드가 없음
   - WebView는 "시작 중..." 상태에서 영원히 대기하다가, 프로세스가 종료되면 `serverStopped`를 받아 "중지됨"으로 전환
   - 서버가 정상 실행되어도 UI는 계속 중지 상태로 표시됨

2. **포트 충돌 오류 무음 처리**
   - 이미 실행 중인 서버가 포트 3000을 점유하고 있을 때, 새 프로세스는 `EADDRINUSE`로 즉시 종료
   - stderr의 에러 메시지는 단순 `Logger.warn`으로만 기록되고 사용자에게 표시되지 않음
   - 사용자는 서버가 왜 시작되지 않는지 알 수 없음

### 수정 내용
| 변경 | 파일 | 설명 |
|------|------|------|
| `serverStarted` 전송 추가 | `CanvasPanel.ts:819` | `cp.spawn()` 직후 WebView에 서버 시작 상태 전송 |
| 포트 충돌 감지 | `CanvasPanel.ts:798-800` | stderr에서 `EADDRINUSE`/`already in use`/`listen` 패턴 감지 시 사용자에게 오류 메시지 표시 |
| 비정상 종료 처리 | `CanvasPanel.ts:812-816` | exit code가 0이 아닌 경우 `serverError` 메시지로 사용자 알림 |

### 영향 범위
- **파일:** `src/webview/CanvasPanel.ts` (handleStartServer 메서드 내부, 5라인 수정)
- **모듈:** Collaboration Server Process (v0.3.30 신규 기능)
- **테스트:** 수동 확인 필요 (자동 테스트 미존재)

---

## 🧩 GUI 계정 관리 기능 (2026-06-09)

### 필요성
기존에는 `scripts/create-account.js` CLI로만 계정 생성 가능했고, 계정 삭제/비밀번호 변경은 아예 불가능. `test1` 계정의 비밀번호를 알 수 없어 로그인 실패.

### 추가된 기능

| 기능 | API | 설명 |
|------|-----|------|
| 계정 목록 조회 | `GET /api/admin/accounts` | 전체 계정 목록 반환 |
| 계정 생성 | `POST /api/admin/create-account` | username + password로 계정 생성 |
| 계정 삭제 | `POST /api/admin/delete-account` | username으로 계정 삭제 |
| 비밀번호 변경 | `POST /api/admin/change-password` | username + newPassword로 변경 |

### 수정 파일

| 파일 | 변경 |
|------|------|
| `src/core/collaboration/AccountManager.ts` | `deleteAccount()`, `changePassword()`, `getAllAccounts()` 메서드 추가 |
| `src/server/standalone.ts` | 계정 관리 API 라우트 4개 추가 (localhost only) |
| `ui/index.html` | 서버 패널 하단에 계정 관리 UI 추가 (목록/생성/삭제/PW 변경) |
| `src/webview/CanvasPanel.ts` | WebView 메시지 핸들러 추가 (createAccount, deleteAccount, changePassword, loadAccounts) |

### UI 구성
- 서버 패널 내 "계정 관리" 섹션
- 계정 목록 (username, userId, PW 변경 입력, 삭제 버튼)
- 새 계정 생성 폼 (username + password 입력 + 생성 버튼)
- 동작 결과 메시지 표시
- 브라우저 모드와 VSCode WebView 모드 모두 지원

### 기본 계정
| Username | Password |
|----------|----------|
| `test1` | `test1` |
| `member1` | `member1` |

---

## 🐛 Hotfix: Account Management Host/Port 하드코딩 문제 (2026-06-09)

### 문제
VSCode WebView의 계정 관리(생성/삭제/비밀번호 변경/목록 조회)가 `localhost:3000`으로 하드코딩되어 있어, 원격 환경(VSCode Remote SSH, Fedora VM 등)에서 서버가 다른 IP(예: 192.168.0.240)에서 실행 중일 때 요청이 잘못된 주소로 전송됨.

- **증상:** 계정 생성/변경이 실패하고 로그인 시 "Invalid credentials" 발생
- **원인:** `CanvasPanel.ts`의 `handleCreateAccount`, `handleDeleteAccount`, `handleLoadAccounts`, `handleChangePassword`가 모두 `hostname: 'localhost', port: 3000` 고정

### 수정 내용
| 변경 | 파일 | 설명 |
|------|------|------|
| `_connHost`/`_connPort` 필드 추가 | `CanvasPanel.ts` | 로그인 성공 시 또는 메시지로부터 받은 host/port 저장 |
| `handleLogin`에 저장 로직 추가 | `CanvasPanel.ts:924` | 로그인 성공 시 `_connHost`, `_connPort` 저장 |
| WebView 메시지에 host/port 포함 | `ui/index.html` | `createAccount`, `deleteAccount`, `loadAccounts`, `changePassword` 메시지에 연결 폼의 host/port 전송 |
| Extension 핸들러에서 host/port 수신 | `CanvasPanel.ts:467-478` | 메시지에 host/port가 있으면 `_connHost`/`_connPort` 갱신 후 처리 |

### 변경된 파일
- `src/webview/CanvasPanel.ts` — 4개 계정 관리 메서드의 hostname/port를 `this._connHost`/`this._connPort`로 변경, 메시지 핸들러에 host/port 저장 로직 추가
- `ui/index.html` — `getConnHostPort()` 유틸리티 함수 추가, 4개 계정 관리 함수에서 WebView 메시지에 host/port 포함

### 기본 계정 (재설정)
| Username | Password |
|----------|----------|
| `test1` | `test1pwd` |
| `member1` | `member1` |

---

## 🐛 Hotfix: 브라우저 빈 캔버스 + VSCode WebView 접속 불가 (2026-06-10)

### 문제
1. **브라우저(standalone)**: 서버 접속 성공 but 캔버스에 아무것도 표시되지 않음
2. **VSCode WebView**: 유저 ID/비밀번호 초기화되어 있고, 접속 시도 시 "접속중"에서 넘어가지 않음

### 원인

#### 원인 1: `ui/canvas-engine.js`에 canvas CSS 크기 설정 누락
`ui/`와 `demo/` 디렉토리의 `canvas-engine.js`가 서로 다른 버전으로 분기. `ui/` 버전에서 `canvas.style.width/height`를 설정하지 않아 초기 렌더링 시 canvas가 0px로 표시됨.

| 위치 | `ui/canvas-engine.js` (v0.3.24) | `demo/canvas-engine.js` (v0.3.09) |
|------|-------------------------------|-------------------------------|
| resize canvas | `this.canvas.width/height`만 설정 | `this.canvas.style.width/height`도 설정 |
| 높이 fallback | `if(height===0) height = innerHeight-100` (음수 가능) | `if(height<100) height = 400` (최소값 보장) |

#### 원인 2: 확장 디렉토리에 `project_state.json` 없음
- `.vscodeignore`에 `demo/**`가 있어 VSIX 패키징 시 `demo/data/` 전체가 제외됨
- 브라우저 모드의 canvas-engine.js가 `/data/project_state.json`을 fetch → 404 → 빈 캔버스
- VSCode WebView도 extension의 `sendProjectState()`가 0 nodes일 때 "Amnesia Guard"에 의해 `projectState` 메시지 차단

#### 원인 3: 두 개의 `accounts.json` 분기
- **프로젝트 경로**: `/home/dogsinatas/TypeScript_project/antigravity-extension-vis/demo/data/accounts.json`
- **확장 경로**: `/home/dogsinatas/.vscode/extensions/synapse-team.synapse-visual-architecture-0.3.30/demo/data/accounts.json`
- standalone 서버(PID 98203)는 확장 경로의 accounts.json 사용 → member1 누락, userId 불일치

### 수정 내용

#### 수정 1: canvas-engine.js CSS 크기 + 최소 높이 패치
| 변경 | 파일: `ui/canvas-engine.js` | 설명 |
|------|---------------------------|------|
| `style.width/height` 추가 | line 1650-1651 | `this.canvas.style.width/height` 명시적 px 설정 |
| 최소 높이 보장 | line 1639 | `height = Math.max(height, 400)` |

#### 수정 2: 프로젝트 데이터 파일 확장 디렉토리로 복사
```bash
cp project/demo/data/project_state.json extension/demo/data/
cp project/demo/data/synapse_history.json extension/demo/data/
cp project/demo/data/accounts.json extension/demo/data/
```

#### 수정 3: standalone 서버 재시작
- 기존 PID 98203 종료 후 새 프로세스(PID 172307) 시작
- 브라우저 렌더링: 484 nodes, 190 edges, 20 clusters 로드 확인
- 로그인: test1/test1pwd, member1/member1 모두 정상

### 변경된 파일
- `ui/canvas-engine.js` — `style.width/height` 명시적 설정, 최소 높이 400px 보장

### 영향 범위
- **브라우저 모드**: `/data/project_state.json` fetch 성공 → 캔버스에 그래프 표시
- **VSCode WebView**: `Reload Window` 후 캔버스 정상 렌더링

### 잔여 과제
- `ui/`와 `demo/` 디렉토리의 `canvas-engine.js` 버전 분기 해소 (어느 쪽을 기준 구현으로 할지 결정)
- standalone 서버 data root 경로 설계 개선: 확장 패키지 내 `demo/data`가 아닌 프로젝트 디렉토리의 data를 읽도록 수정

---

## 🔧 Hotfix v0.3.30d — `saveConnState is not defined` Scoping Bug

### 문제
`ui/index.html`에서 `saveConnState()`가 `loginResult` 핸들러(`index.html:1604`)에서 호출되었으나, 함수 정의는 `DOMContentLoaded` 콜백 내부(`index.html:1856`)에 있어 스코프 불일치로 `ReferenceError` 발생. 변수 `connHost`, `connPort`, `connDot`, `connStatus`도 동일한 스코프 문제로 `loginResult` 핸들러에서 접근 불가.

### 수정 사항
1. **함수 및 DOM 참조 최상위 스코프로 이동** (`index.html:1357-1388`)
   - `connPanel`, `connHost`, `connPort`, `connUser`, `connPass`, `connError`, `connStatus`, `connDot` → `let` 선언을 `DOMContentLoaded` 밖으로 이동
   - `connectedUser` → 최상위 `let connectedUser = null`
   - `updateConnStatus()`, `loadConnState()`, `saveConnState()`, `clearConnState()` → 최상위 함수로 이동
   - 내부 DOM 할당문 (`document.getElementById(...)` 등)은 `DOMContentLoaded` 내에 유지 (값 할당 시점 보장)
2. **중복 `let connectedUser = null` 제거** — `DOMContentLoaded` 내부 선언이 최상위 변수를 가리던 문제 해결
3. **`getConnHostPort()` 기본값 제거** — `host`가 빈 문자열일 때 `localhost`로 fallback하지 않음. WebView message handler가 `message.host` falsy 체크로 빈 값을 skip하도록 이미 설계됨 (CanvasPanel.ts:482)
4. **재패키지**: `synapse-visual-architecture-0.3.30.vsix` (559 files, 28.84MB)

### 영향 범위
- `loginResult` 핸들러가 `saveConnState()` 및 DOM 참조를 정상적으로 접근 가능
- 로그인 후 "접속중" 상태에서 진행되지 않는 hang 해결
- 계정 관리 UI가 `conn-host`/`conn-port` 값을 사용하여 올바른 호스트로 API 요청 전송

---

## 🔧 Hotfix v0.3.30e — `window.*` 전역 할당으로 스코프 문제 최종 해결

### 문제 재진단
Hotfix v0.3.30d에서 `saveConnState`/`updateConnStatus` 함수와 DOM 참조 변수를 `DOMContentLoaded` 밖 최상위 스코프로 이동했으나, VSCode WebView의 VM 컨텍스트에서 `ReferenceError: saveConnState is not defined`가 계속 발생 (`VM8:275`). 함수 선언(`function saveConnState`)이 특정 WebView 실행 모드에서 스코프 체인에 정상적으로 포함되지 않는 것이 근본 원인으로 추정.

### 수정 사항
1. **모든 연결 상태 변수/함수를 `window.*` 명시적 전역 할당으로 변경**
   - `let connPanel, connHost, ...` → `window.connPanel = null; window.connHost = null; ...`
   - `function saveConnState(...) {...}` → `window.saveConnState = function(...) {...}`
   - `function updateConnStatus(...) {...}` → `window.updateConnStatus = function(...) {...}`
   - `function loadConnState(...) {...}` → `window.loadConnState = function(...) {...}`
   - `function clearConnState(...) {...}` → `window.clearConnState = function(...) {...}`
2. **모든 참조를 `window.*`로 일괄 변경** — DOMContentLoaded 내부의 `connPanel.classList.toggle(...)`, `connError.textContent = ...`, `connDot.className = ...`, `clearConnState()` 등 모든 사용처 업데이트
3. **`loginResult` 핸들러** (`index.html:1656`)에서 `window.saveConnState(...)`, `window.updateConnStatus(...)` 호출
4. **재패키지 및 설치**: `synapse-visual-architecture-0.3.30.vsix` → `code --install-extension --force`

### 영향 범위
- 모든 JavaScript 실행 컨텍스트 (VSCode WebView, 브라우저)에서 `saveConnState` 접근 보장
- 로그인 hang 완전 해결

---

## Task B — Source of Truth 정리 (2026-06-10)

### 결정
| 항목 | 값 |
|------|-----|
| Canvas Engine Source of Truth | `ui/canvas-engine.js` |
| `demo/canvas-engine.js` 상태 | 동기화된 산출물 (generated artifact) |
| 근거 | `ui/`에만 버그 픽스, 경쟁 조건 방어, 외부 레이어, 클러스터 충돌 해결 존재; `demo/`에 단독 기능 없음 |
| diff 규모 | ~150 lines (ui 9182 / demo 9032) |
| 조치 | `ui/canvas-engine.js` → `demo/canvas-engine.js`로 복사 완료 |

### 수정 파일
- `mile_stone/SYNAPSE_v0.3.30_BOOTSTRAP_SPECIFICATION.md` — Known Decisions 섹션 추가 (Transport=REST, CanvasEngine Source=ui/)
- `demo/canvas-engine.js` — `ui/canvas-engine.js`로 덮어쓰기 (동기화)

### 잔여
- 빌드 파이프라인에 `cp ui/canvas-engine.js demo/canvas-engine.js` 포함 필요 (Option A) → `build-guard.js`에 추가 완료
- `demo/` 직접 수정 금지 정책 문서화 완료

---

## Task C — Harvest UI/API (REST + CanvasPanel + HTML Button) (2026-06-10)

### Gate P1-1: HarvestInput 시그니처 확인

`HarvestEngine.harvest(input: HarvestInput)`:

| 필드 | 타입 | REST API 제공 |
|------|------|---------------|
| `submissionId` | `string` | ✅ `req.body.submissionId` |
| `projectUUID` | `string` | ✅ `req.body.projectUUID` |
| `approvedFiles` | `SubmissionFile[]` | ✅ `approvedFiles[]` 또는 `filePaths[]` |
| `originalSnapshot` | `SubmissionSnapshot` | ✅ 서버에서 재구성 |
| `verificationReport` | `VerificationReport` | ✅ 최소 리포트 자동 생성 |

### Gate P3-1: Layer Visibility → approvedFiles 매핑 규칙

| 단계 | 설명 |
|------|------|
| 1 | `canvasEngine.showBaseLayer`, `showUserLayer`, `showExternalLayer` 상태 읽기 |
| 2 | `canvasEngine.nodes`를 필터링 (ON 레이어의 노드만 유지) |
| 3 | 필터링된 노드의 `filePath` 추출 (graph 노드의 고유 경로) |
| 4 | 추출된 `filePath[]`를 REST API에 전송 |
| 5 | 서버에서 SubmissionManager 조회 또는 직접 파일 읽기 → content 획득 |
| 6 | `HarvestEngine.harvest()` 실행 |

### P1 — REST API: `POST /api/collab/harvest`

**파일**: `src/server/standalone.ts`

| 항목 | 내용 |
|------|------|
| 엔드포인트 | `POST /api/collab/harvest` |
| 요청 본문 | `{ submissionId, projectUUID, approvedFiles[] \| filePaths[] }` |
| 응답 | `{ success, result: HarvestResult }` |
| 임포트 추가 | `SubmissionManager`, `HarvestEngine`, `HarvestInput` |
| filePaths 처리 | SubmissionManager 조회 → 실패 시 `fs.readFileSync(projectRoot + fp)` |

### P2 — CanvasPanel.ts: `case 'harvest'` 핸들러

**파일**: `src/webview/CanvasPanel.ts`

| 항목 | 내용 |
|------|------|
| 새로운 case | `case 'harvest':` → `this.handleHarvest(message.submissionId, message.projectUUID, message.filePaths)` |
| handleHarvest() | `http.request` → `POST /api/collab/harvest` → `vscode.postMessage({ command: 'harvestResult', ... })` |
| 타임아웃 | 30초 (파일 쓰기 작업 고려) |

### P3 — Harvest Button in `ui/index.html`

**파일**: `ui/index.html`

| 항목 | 내용 |
|------|------|
| 버튼 ID | `#btn-harvest` |
| 위치 | 서버 패널(`#server-panel .panel-content`), "서버 중지" 버튼 아래 |
| 표시 조건 | `window.connectedUser`가 truthy일 때만 `display: block` |
| 동작 (Browser) | `fetch('/api/collab/harvest')` + 결과 표시 |
| 동작 (WebView) | `vscode.postMessage({ command: 'harvest', ... })` + `harvestResult` 메시지 리스너 |
| `collectVisibleFilePaths()` | canvas engine의 layer visibility flag + node layer 분류 로직을 복제하여 ON 레이어의 노드 filePath 수집 |

### 영향 범위

| 파일 | 변경 내용 |
|------|-----------|
| `src/server/standalone.ts` | SubmissionManager 임포트 추가; `POST /api/collab/harvest` 라우트 (filePaths 지원) |
| `src/webview/CanvasPanel.ts` | `case 'harvest':` + `handleHarvest()` 메서드 (REST 호출 → 결과 postMessage) |
| `ui/index.html` | `#btn-harvest` 버튼 + `#srv-harvest-result` 결과 표시 영역 + `collectVisibleFilePaths()` + 클릭 핸들러 + `updateHarvestButtonVisibility()` |

### 잔여 과제
- **P4-P6**: 동적 클라이언트 레이어 관리 (별도 이정표)

---

## Gate H-1 검증 결과 (2026-06-10)

| Gate | 테스트 | 결과 | 비고 |
|------|--------|------|------|
| H-1A | `POST /api/collab/harvest` (Browser/REST) | ✅ PASS | `{"success":true}`, `filesHarvested:1` |
| H-1B | VSCode WebView Harvest (로그인→버튼→harvestResult) | ⏳ PENDING | 수동 UI 검증 필요 (WebView 콘솔, 버튼 표시 조건) |
| H-1C | `npm run test:unit` 회귀 | ✅ PASS | **110/110** (phase1-9 + transport, 이전 106→110 증가) |
| H-1D | `.synapse/master/` 실제 파일 | ✅ PASS | `demo/.synapse/master/canvas-engine.js` (393KB) 생성 확인 |
| H-1E | `filePaths` 모드 (서버 disk resolve) | ✅ PASS | `filePaths:["canvas-engine.js"]` → disk read → harvest |
| H-1F | `approvedFiles` 모드 (direct content) | ✅ PASS | `approvedFiles:[{filePath:"test.js", content:"..."}]` → disk 미접근 |

### 상세 결과

#### H-1D 파일 확인
```bash
demo/.synapse/
├── master/
│   ├── canvas-engine.js   393KB (내용 정상)
│   └── test.js            21B (approvedFiles 모드)
└── snapshots/
    └── test_h1a/
        └── snapshot.json   (원본 스냅샷 보존)
```

#### H-1F approvedFiles 내용 검증
```javascript
// demo/.synapse/master/test.js
console.log("hello");
```

### H-1B 미완료 사유
H-1B는 VSCode WebView 내 수동 UI 검증 필요:
1. VSCode에서 `Reload Window` 후 로그인
2. 서버 패널 → Harvest 버튼 표시 확인 (`window.connectedUser` truthy)
3. Harvest 실행 → `harvestResult.success === true` 확인
4. `demo/.synapse/master/` 생성 확인
5. 로그아웃 상태 → 버튼 숨김 확인

### v0.3.31 진입 판단

| 조건 | 상태 |
|------|------|
| Browser Harvest 성공 | ✅ |
| VSCode Harvest 성공 | ⏳ (수동 확인 필요) |
| filePaths 모드 성공 | ✅ |
| approvedFiles 모드 성공 | ✅ |
| .synapse/master 실제 생성 확인 | ✅ |
| 106/106 테스트 유지 | ✅ (110/110) |

→ Harvest 노출 작업 완료. v0.3.31 진입 가능 (H-1B 수동 확인 후 최종 확정).
