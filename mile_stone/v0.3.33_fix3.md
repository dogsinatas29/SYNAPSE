v0.3.33_fix3.md
GOAL

Graph 자체를 수정하지 않는다.

nodes
edges
clusters

는 그대로 유지한다.

수정 대상은

Visibility Layer
Heatmap Layer
Interaction Layer

뿐이다.

구현 전략
Phase 1
Visible Cluster Registry 생성

현재

Hide Noise

↓

Visible Node 계산

↓

끝

이다.

추가

Hide Noise

↓

Visible Node Set

↓

Visible Cluster Set

↓

Renderer
Heatmap
Interaction
공유

예시

visibleNodes

생성 후

visibleClusterIds

생성

for node in visibleNodes
{
    visibleClusterIds.add(node.cluster_id);
}
Phase 2
Cluster Renderer 통합

현재

renderClusters()

↓

this.clusters

기준

수정

renderClusters()

↓

visibleClusterIds

기준

즉

if (!visibleClusterIds.has(cluster.id))
{
    continue;
}

중요

cluster 삭제 금지

skip

만 수행

Phase 3
Cluster Bounds 보호

현재

node 전멸

↓

fallback

↓

(0,0)

발생

수정

node 전멸

↓

bounds 생성 안 함

즉

return null;

그리고

if (!bounds)
{
    skip;
}

(0,0) fallback 제거

Phase 4
Heatmap Visibility Contract

현재

clusterFlows

는

all edges

기준

유지

하지만

그리기 전

if (!visibleClusterIds.has(src))
{
    return;
}

if (!visibleClusterIds.has(dst))
{
    return;
}

즉

계산 유지

표시만 제한

Phase 5
Interaction Layer 통합

현재

getClusterHeaderAt()

getClusterAt()

가

this.clusters

전체 기준

수정

visibleClusterIds

기준

예시

if (!visibleClusterIds.has(cluster.id))
{
    continue;
}
Phase 6
Empty Cluster Drag 제거

현재

draggedEmptyCluster

경로 존재

Fix 이후

visibleClusterIds

기준으로만 렌더

되므로

실질적으로

visible cluster
=
visible node 존재

상태

따라서

draggedEmptyCluster

경로는 사실상 사용되지 않음

하지만

삭제하지 않는다.

이유

Buffer Cluster
Reserve Cluster
LOD

미래 호환성

구현 결과
Hide Noise

기존

노드 숨김

클러스터 남음

히트맵 남음

수정 후

노드 숨김

↓

Visible Cluster Set 재계산

↓

클러스터 숨김

↓

히트맵 숨김
External Ref OFF

기존

external node 제거

↓

cluster_ghosts center

↓

(0,0)

↓

heatmap 응집

수정 후

external cluster

↓

Visible Cluster Set 제외

↓

Heatmap draw skip

(0,0) 중력원 소멸

이 방식의 장점

건드리는 영역

canvas-engine.js

거의 전부

건드리지 않는 영역

StateManager
ClusterBuilder
NodeBuilder
EdgeBuilder
Buffer Cluster
Reserve Cluster
project_state.json

전부 유지

즉 이번 Fix는

데이터 구조 수정

X

렌더링 계약 수정

O

입니다.

이게 현재까지 발견된 모든 현상(Hide Noise, Empty Cluster, Heatmap 응집, External Ref OFF)을 가장 적은 변경으로 동시에 해결하는 경로입니다.
