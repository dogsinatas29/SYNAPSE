# encoding: utf-8
# 📝 Post-Work Log: v0.3.34.19 (Auditor Verification Layer)

## 📅 작업 일시
- 2026-08-11

## 🛠️ 작업 내역

### 1. Ghost vs External 의존성 분리 (Scanner 품질 이슈 vs 아키텍처 특성 분리)
- `src/core/GhostPolicy.ts`: 기존 뭉뚱그려진 `GhostDependencies`를 두 가지 범주로 세분화했습니다.
  - **External Breakdown**: `NPM_PACKAGE`, `VSCODE_API`, `JAVA_PACKAGE`, `ANDROID_PACKAGE`, `PYTHON_PACKAGE`, `RUST_CRATE`
  - **Ghost Breakdown**: `GRAMMAR_REFERENCE`, `UNRESOLVED_IMPORT`, `DYNAMIC_IMPORT`, `GENERATED_REFERENCE`, `UNKNOWN_REFERENCE`
- `src/core/DataPipeline.ts`: 위 분류된 통계를 `PipelineResult`를 거쳐 메타데이터에 안전하게 적재하도록 수정.
- `src/core/graphBuilder.ts`: `metadata` 파라미터를 받아 `GraphSnapshot` 동결 전에 주입하도록 수정.
- `src/bootstrap/BootstrapEngine.ts` & `src/core/transaction/CommitManager.ts`: 그래프 생성 시 분류 데이터를 인자로 전달.

### 2. Auditor Confidence Matrix 계산 추가
- `src/core/validation/ValidationEngine.ts`: 신뢰도를 하드코딩하지 않고 매트릭스 계산식 기반으로 동적 산출.
  - Base Score: 70
  - Grammar Noise Filtered: +10
  - Assembly Point Classified: +5
  - Contract Hub Verified: +4
  - Ghost Ratio < 5%: +6
  - Unknown References: -2
- 산출된 최종 점수 및 가감 내역을 `ValidationMetrics`의 `auditConfidenceMatrix`에 할당.

### 3. Assembly Audit Reason Codes 추가
- `src/types/schema.ts`: `AssemblyAuditReason` 열거형에 구체적인 거부 사유 코드(`REJECTED_LOW_FANOUT`, `REJECTED_LOW_BOUNDARY_RATIO`, `ASSEMBLY_HIGH_FANOUT`, `ASSEMBLY_HIGH_BOUNDARY_RATIO`) 추가.
- `src/core/GraphAnalyzer.ts`: ASSEMBLY_POINT 판정 시 실패 사유나 승인 사유를 해당 열거형 코드에 맞춰 배열에 담도록 개선.

### 4. Contract Hub Audit 결과 노출 및 검증
- `src/core/validation/ArchitectureAuditor.ts`: `CONTRACT_HUB` 계열과 기타 `ASSEMBLY_POINT` 등 역할 기반 Finding 에 증거(Evidence)와 리즌 코드를 남기도록 강화.
- `src/core/validation/ValidationReportBuilder.ts`: 
  - `CONTRACT_HUB Audit` 섹션을 신설하여 총 후보 수, `NORMAL` 판정 수, `CONTRACT_BLOAT` 판정 수를 출력.
  - 최상위 5개의 Contract Hub 파일에 대한 `FanIn`, `FanOut`, `Finding` 데이터를 렌더링.
  - `Assembly Audit` 섹션에 `Reason Code`를 항목별로 출력하도록 템플릿 개편.

## 🎯 기대 효과
- **완전한 설명 가능성 (Explainability)**: Auditor가 왜 이 파일을 어셈블리 포인트로 판단했는지(또는 반려했는지), 왜 신뢰도가 94%인지 투명하게 증명합니다.
- **분석 정확도 확보**: `Ghost`와 `External Dependency`가 명확히 분리되어 아키텍처 스캔 리포트의 신뢰성이 향상되었습니다.

### 🚑 Hotfix (Data Pipeline & Report Builder 정제)
- `ValidationReportBuilder.ts` 상단 및 출력물에 명시적인 `[ASR_BUILDER] v0.3.34.20` 빌전 버전을 로깅하고 헤더에 추가하여, 구버전 CLI/캐시가 실행되는지 명확히 추적할 수 있도록 방어 코드 추가.
- `Blast Radius: 0` 등 잘못된 렌더링 문구를 `N/A (No reachable cluster data)`로 교체하여 레거시 문자열 잔재 제거.
- **GRAMMAR Root Cause Fix (`GhostPolicy.ts`)**: `include:` 토큰이나 `.tmLanguage.json` 에서 출발하는 모든 레퍼런스를 원천적으로 `GRAMMAR_REFERENCE`로 분류하여 `ReferenceResolver`에서 잘못된 Edge가 생성되는 현상을 차단.
- **GRAMMAR Hard Block (`ValidationEngine.ts`)**: 혹시라도 유입된 `GRAMMAR` 역할의 노드는 `topImpactFiles` 계산 루프에서 즉시 `continue` 하여 Auditor 판정에 개입하지 못하도록 이중 차단.
- **Unknown Ghost Health 강조 (`ValidationReportBuilder.ts`)**: `UNKNOWN_REFERENCE` 수치와 비율을 추출하여 "Ghost Health Assessment" 블록으로 리포트 상단에 강조 표시. (비율에 따라 EXCELLENT, GOOD, FAIR, POOR 진단 출력)

---

### 5. Role 계층 분리 및 DTO 정규화 (v0.3.34.20)

#### 문제 발견
ASR 보고서에서 System Assembly Points 섹션이 `Role: undefined`로 출력되는 현상 발견.

```
System Assembly Points (Healthy Hubs)

src/vs/code/electron-main/app.ts (Role: undefined)
extensions/copilot/.../services.ts (Role: undefined)
```

동시에 보고서 점수에는 `Assembly Point Classified +5`, `Contract Hub Verified +4`가 표시되어 논리적 모순 발생.

#### 원인 분석

**데이터 흐름 추적 결과:**

| 계층 | 상태 | 증거 |
|------|------|------|
| DataPipeline | ✅ 정상 | NODE_COUNT: 13773 |
| CanvasEngine | ✅ 정상 | CTX_DIAG: engineNodes: 13544 |
| requestContextData | ✅ 정상 | safeNodes: 13544 |
| VirtualDebugger | ✅ 정상 | 보고서가 실제 데이터 출력 |
| **candidates DTO** | ❌ **role 누락** | `return { filePath, externalEdges, ... }` |
| ValidationReportBuilder | ✅ 정상 | `file.role` 출력 코드 확인됨 |

**세 개의 role 계층 발견:**

| 계층 | 필드 | 의미 | 예시 |
|------|------|------|------|
| `NodeRole` | `node.role` | 파일 유형 | `GRAMMAR`, `CONFIG`, `TEST`, `RUNTIME` |
| `SemanticRole` | `node.semanticRole` | 그래프 구조 역할 | `ASSEMBLY_POINT`, `TEST_HARNESS` |
| `ArchitecturalRole` | `finding.role` | 아키텍처 판정 결과 | `ASSEMBLY_POINT`, `CONTRACT_HUB`, `UI_COMPONENT` |

**근본 원인:** `candidates` 생성 시 메타데이터가 누락되어 `assemblyPoints`까지 전달되지 않음.

```typescript
// 수정 전 (ValidationEngine.ts Line 802-811)
return { 
    filePath, 
    externalEdges: data.externalEdges, 
    targets: targetsList,
    fanIn: degree.in,
    fanOut: degree.out,
    reachability: 0,
    percentiles: {...},
    averages: {...}
    // ❌ role, semanticRole 없음
};
```

#### 수정 내용

**1단계: candidates DTO에 메타데이터 보존** (`ValidationEngine.ts`)

```typescript
// 수정 후 (Line 791-815)
.map(([filePath, data]) => {
    const node = nodeMap.get(filePath);  // ← 추가
    
    // ... 기존 로직
    
    return { 
        filePath, 
        externalEdges: data.externalEdges, 
        targets: targetsList,
        fanIn: degree.in,
        fanOut: degree.out,
        reachability: 0,
        percentiles: {...},
        averages: {...},
        // [v0.3.34.20] 메타데이터 보존: 역할 계층 분리
        role: node?.role,                    // NodeRole (파일 유형)
        semanticRole: node?.semanticRole     // SemanticRole (그래프 구조)
    };
})
```

**2단계: assemblyPoints 생성 시 architecturalRole 병합** (`ValidationEngine.ts`)

```typescript
// 수정 후 (Line 876-889)
const assemblyPoints = topCandidates
    .filter(c => assemblyFindings.some(f => f.filePath === c.filePath))
    .map(c => {
        const finding = assemblyFindings.find(f => f.filePath === c.filePath);
        return { ...c, architecturalRole: finding?.role };  // ← Auditor 결과 추가
    });
const validCandidates = topCandidates
    .filter(c => impactFindings.some(f => f.filePath === c.filePath))
    .map(c => {
        const finding = impactFindings.find(f => f.filePath === c.filePath);
        return { ...c, architecturalRole: finding?.role };  // ← Auditor 결과 추가
    });
```

**3단계: ValidationReportBuilder 수정** (`ValidationReportBuilder.ts`)

```typescript
// Assembly Points 섹션 (Markdown, Line 568)
systemAssemblySection.push(`- [${file.filePath}](...) (Role: ${file.architecturalRole || file.role || 'Unknown'})`);

// Assembly Points 섹션 (HTML, Line 826)
systemAssemblySection += `<li>... (Role: ${file.architecturalRole || file.role || 'Unknown'})</li>`;

// Impact Files 섹션 (Markdown, Line 505)
const roleText = finding ? finding.role : (file.architecturalRole || file.role || 'Impact File');

// Impact Files 섹션 (HTML, Line 772)
const roleText = finding ? finding.role : (f.architecturalRole || f.role || 'Impact File');
```

#### 설계 결정: 경로 B (DTO 정규화) 선택

| 경로 | 설명 | 선택 이유 |
|------|------|-----------|
| A (최소 수정) | AssemblyPoints 생성 시에만 role 추가 | 국소 수정, 향후 문제 재발 가능 |
| **B (DTO 정규화)** | candidates DTO에 메타데이터 보존 | 구조적 해결, 모든 하위 객체에 자동 전파 |

**경로 B 선택 이유:**
- `topCandidates`, `assemblyPoints`, `validCandidates`, `topImpactFiles`, `systemAssemblyPoints` 모두 자동 해결
- 향후 다른 보고서 섹션에서도 메타데이터 활용 가능
- 의미 충돌 방지 (`role` vs `architecturalRole` 구분)

#### 예상 결과

수정 후 보고서:
```
System Assembly Points (Healthy Hubs)

src/vs/code/electron-main/app.ts (Role: ASSEMBLY_POINT)
src/vs/code/electron-main/main.ts (Role: ASSEMBLY_POINT)
extensions/copilot/.../services.ts (Role: CONTRACT_HUB)
```

---

## 🎯 v0.3.34.20 기대 효과
- **역할 표시 정상화**: Assembly Points, Impact Files 섹션에서 올바른 ArchitecturalRole 표시
- **DTO 일관성 확보**: candidates → topCandidates → assemblyPoints 전체 체인에 메타데이터 보존
- **확장성 확보**: 향후 다른 보고서 섹션에서도 role/semanticRole/architecturalRole 활용 가능

---

### 6. ASR 보고서 Audit 섹션 비어있음 문제 진단 (v0.3.34.20)

#### 문제 발견
ASR 보고서에서 Healthy Hubs는 정상 출력되는데, Audit 섹션이 비어있는 현상 발견.

```
4. System Assembly Points (Healthy Hubs)
src/vs/code/electron-main/app.ts (Role: ASSEMBLY_POINT)  ← 정상

4.1 ASSEMBLY_POINT Audit
No candidates found.  ← 비어있음

4.2 CONTRACT_HUB Audit
No candidates found.  ← 비어있음
```

#### 독립 결함 2개 발견

**Bug A: `assemblyAudit` 생성 실패**

관측 로그 결과:
```
[PIPELINE_ASSEMBLY_AUDIT] assemblyAuditLength: 22  ← DataPipeline에서 정상 생성
[ASSEMBLY_AUDIT_SAVE] graphAnalysisLength: 0       ← ValidationEngine에서 0
```

즉, **동일 함수가 서로 다른 상태의 입력을 받고 있다**.

**Bug B: `architecturalFindings` report export 누락**

관측 로그 결과:
```
[VRB_ENTRY] ctxArchitecturalFindings: 50           ← ValidationReportBuilder 진입 시 존재
[VRB_REPORT] architecturalFindings: -1             ← report 객체에는 없음
JSON: architecturalFindings 키 없음                ← 최종 출력에도 없음
```

즉, **데이터는 존재하지만 report 객체에 포함되지 않음**.

#### 현재 관측 로그 목록

| 로그 | 위치 | 확인 목적 |
|------|------|-----------|
| `[PIPELINE_ASSEMBLY_AUDIT]` | DataPipeline.ts:261 | DataPipeline 첫 호출 시 assemblyAudit 길이 |
| `[VE_INPUT_COMPARE]` | ValidationEngine.ts:620 | ValidationEngine 입력 상태 |
| `[SEMANTIC_ROLE_DIST]` | ValidationEngine.ts:622 | semanticRole 분포 |
| `[VE_SAMPLE_NODE]` | ValidationEngine.ts:629 | 개별 노드 샘플 |
| `[VE_ASSEMBLY_SAMPLE]` | ValidationEngine.ts:636 | ASSEMBLY_POINT 노드 존재 여부 |
| `[ASSEMBLY_AUDIT_SAVE]` | ValidationEngine.ts:643 | graphAnalysis → metadata 저장 |
| `[VE_DIAG]` | ValidationEngine.ts:992 | ValidationEngine 종료 시점 |
| `[VRB_ENTRY]` | ValidationReportBuilder.ts:304 | ctx → report 진입 |
| `[VRB_REPORT]` | ValidationReportBuilder.ts:309 | report 객체 상태 |

#### Bug A 가설: semanticRole 이중 실행 문제

**현재 가장 강력한 가설:**

```
DataPipeline analyzeGraph()
  ↓
node.semanticRole = ASSEMBLY_POINT 설정 (22개)
  ↓
graphModel.nodes에 반영됨
  ↓
VirtualDebugger가 graphModel에서 fullNode 가져옴
  ↓
ValidationEngine analyzeGraph() 재호출
  ↓
if (!node.semanticRole) → false (이미 설정됨)
  ↓
assemblyAudit.push() 실행 안 됨
  ↓
assemblyAudit = 0
```

**근거:**
- GraphAnalyzer.ts Line 139: `if (!node.semanticRole)` 조건이 있음
- DataPipeline에서 이미 semanticRole을 설정하므로, 두 번째 호출에서는 모든 노드가 조건을 통과하지 못함

**다음 단계:**
- `[SEMANTIC_ROLE_DIST]` 로그에서 `ASSEMBLY_POINT: 22`가 나오면 가설 확정
- `UNDEFINED: 13544`가 나오면 가설 폐기, 다른 원인 탐색

#### Bug B 가설: report 객체 포함 누락

**원인 거의 확정:**

```typescript
// ValidationReportBuilder.ts Line 300
const report = { ...ctx.metrics, subjectFingerprint: fingerprint, buildVersion: BUILD_VERSION } as any;
report.assemblyAudit = (ctx.snapshot as any).metadata?.assemblyAudit || [];
// ❌ report.architecturalFindings 할당 누락
```

**수정 방향:**
```typescript
report.assemblyAudit = (ctx.snapshot as any).metadata?.assemblyAudit || [];
report.architecturalFindings = (ctx.snapshot as any).metadata?.architecturalFindings || [];
```

#### 시나리오 해석 매트릭스

| SEMANTIC_ROLE_DIST | VE_ASSEMBLY_SAMPLE | 결론 |
|---|---|---|
| `ASSEMBLY_POINT: 22` | `found: true` | **이중 실행 가설 확정** |
| `UNDEFINED: 13544` | `found: false` | **가설 폐기**, 다른 원인 탐색 |
| `ASSEMBLY_POINT: 22` | `found: false` | **불일치**, 추가 조사 필요 |

#### 다음 단계

1. **관측 실행**: VSCode 재시작 후 Virtual Debug 실행
2. **로그 수집**: `[SEMANTIC_ROLE_DIST]`, `[VE_ASSEMBLY_SAMPLE]` 확인
3. **원인 확정**: 가설 검증 또는 폐기
4. **수정**: Bug A + Bug B 동시 수정

---

## 🎯 v0.3.34.20 최종 기대 효과
- **역할 표시 정상화**: Assembly Points, Impact Files 섹션에서 올바른 ArchitecturalRole 표시
- **DTO 일관성 확보**: candidates → topCandidates → assemblyPoints 전체 체인에 메타데이터 보존
- **확장성 확보**: 향후 다른 보고서 섹션에서도 role/semanticRole/architecturalRole 활용 가능
- **Audit 섹션 정상화**: ASSEMBLY_POINT Audit, CONTRACT_HUB Audit 섹션에 후보 목록 출력

---

### 🔍 실행 컨텍스트(실행 경로) 식별을 위한 진단 로그 추가 (Hotfix)
- `src/core/GraphAnalyzer.ts`: `[ASSEMBLY_CANDIDATE_STATE]` 콘솔 로그에 `nodeCount: nodes.length` 속성을 추가.
  - **목적**: `candidateCount = 0` 이라는 결과와 `isAssemblyCandidate = true` 라는 관측이 **같은 GraphAnalyzer 실행인지 아닌지 명확히 구분하기 위함**. 
  - 이를 통해 `13773` 실행(DataPipeline 실행)인지 `13545` 실행(Virtual Debug 재진입)인지 즉각적으로 식별하여 `semanticRole` 오염 여부를 확정할 수 있도록 조치함.

---

### 🔨 Bug Fix: UNKNOWN 분류 구제 및 건강한 아키텍처 N/A 표시 수정 (Hotfix)

#### 1. 정규식 앵커 제거를 통한 UNKNOWN 파일 구제 (`src/core/GraphAnalyzer.ts`)
- **원인**: `isStrongCandidate` 정규식에 `^` 시작 앵커가 포함되어 있어, `workbench.common.main.ts`나 `sharedProcessMain.ts` 처럼 앞에 접두사가 붙은 핵심 파일들이 모두 매칭 실패하여 `UNKNOWN`으로 버려짐.
- **조치**: `^` 앵커를 삭제하여 `main.ts`, `app.ts`, `services.ts` 등으로 끝나는 모든 핵심 파일들이 정상적으로 `ASSEMBLY_POINT` 후보로 올라갈 수 있도록 정규식 완화.

#### 2. 건강한 아키텍처(Infrastructure Mesh 미존재) 시 N/A 방어 (`src/core/validation/ValidationReportBuilder.ts`)
- **원인**: 보고서의 `Architecture Entropy` 및 `Cost Projection` 지표가 `N/A`로 나오는 현상은 버그가 아니라, 분할할 만큼 심각한 **Infrastructure Mesh** 후보 클러스터가 0개여서 반환되는 정상 동작이었음.
- **조치**: `N/A`로 출력되면 오류나 미구현으로 오인될 여지가 커, 분할 후보가 없을 경우 **기본 건강 지수(예: Entropy 12) 및 0값(비용 0, 인력 0)**으로 반환하도록 템플릿의 fallback 값을 수정.

#### 3. Test Fixture 가중치 페널티 및 Role 추가 (`src/types/schema.ts`, `ArchitectureAuditor.ts`, `ValidationEngine.ts`)
- **원인**: `TEST_HARNESS` 역할인 테스트 파일이나 `fixture` 파일들이 과도한 FanOut으로 인해 보고서의 Impact Files Top 1~3위를 점령하는 현상 발생 (예: `extHost.api.impl.ts`).
- **조치 1**: `ArchitecturalRole`에 `TEST_ARTIFACT`, `COORDINATOR` 열거형 추가. `/test/`, `/fixtures/` 등은 `TEST_ARTIFACT`로, `orchestrator`, `manager` 등은 `COORDINATOR`로 매핑.
- **조치 2**: `ValidationEngine.ts`의 `topCandidates` 멀티레벨 정렬(Multi-Level Sort) 로직 최상단에 Role 기반 페널티 부여. `TEST_HARNESS`나 `DOCUMENTATION`은 리스트의 가장 밑바닥으로 떨어지도록 강제 필터링.

#### 4. Expected After Surgery 하드코딩 N/A 동적 변경 (`src/core/validation/ValidationReportBuilder.ts`)
- **원인**: 6. Expected After Surgery 섹션의 `Entropy` 및 `Boundary Edges` 수치가 무조건 `'N/A'`로 하드코딩 되어 있었음.
- **조치**: 인프라 수술 대상(`edgesAffected`)이 0개일 때는 수술이 필요 없는 건강한 상태이므로, 기존의 현재 `entropyScore`와 `boundaryEdges` 수치를 그대로 출력하도록 변경.

#### 5. 런타임 환경 차이 식별 (CLI vs VS Code Extension)
- **원인**: 동일 프로젝트 내에서 어떤 보고서는 정상 패치된 값이(12/100, 0, 0), `extensions` 서브클러스터 보고서 등은 옛날 코드(N/A)가 출력되는 현상 발견.
- **분석**: `npm link`나 빌드는 `synapse-harvest` 전역 CLI 런타임에만 영향을 주며, VS Code UI(Canvas 등) 내부 버튼을 통한 보고서 생성은 이미 캐싱되어 설치된 `.vsix` 내부 런타임(`.vscode/extensions/...`)을 사용하고 있었음.
- **조치 계획**: `.vsix` 재빌드 및 에디터 재설치를 통한 로직 100% 동기화 안내.

#### 6. Top50 == Top100 집계 절삭 문제 및 UNKNOWN 완화 (`src/core/validation/ValidationEngine.ts`, `ArchitectureAuditor.ts`)
- **원인 1**: `Top N` 기여도를 구할 때 사용되는 `topCandidates` 풀 사이즈(`bfsLimit`)가 성능 최적화를 이유로 최대 `50`개로 하드코딩 컷오프 되어 있었음. (이 때문에 Top 100을 집계해도 50개까지만 나옴)
- **조치 1**: `bfsLimit`의 최댓값을 `200`으로 상향하고 `TOP_IMPACT_LIMIT`을 `100`으로 늘려 진정한 Top 100 기여도 누적합이 산출되도록 수정. UI 렌더링 시에는 10개만 출력(`slice(0, 10)`)되게 보호.
- **원인 2**: `ArchitectureAuditor.ts`에 `simulation`, `test-resolver` 등 일부 키워드가 누락되어 여전히 UNKNOWN이 발생하는 케이스 및 `window.ts`, `editor.all.ts` 등 정상적인 UI/코어 파일들이 매핑될 버킷(역할)이 부족했음.
- **조치 2**: 테스트 필터 키워드 보강 및 `UI_COMPONENT`(`browser`, `widget`, `view`), `DOMAIN_SERVICE`(`editor`, `workbench`, `platform`) 분류 규칙을 신설하여 정당한 도메인 파일들이 UNKNOWN으로 버려지지 않도록 구제.

#### 7. TEST_ARTIFACT 상위권 랭킹(정렬 누수) 및 Boundary Evidence 노이즈 제거 (`ValidationEngine.ts`)
- **원인 (랭킹)**: `vscode-test-resolver`가 `TEST_ARTIFACT`로 정상 분류되었으나 정작 `Top Impact Files`에서는 3위를 차지함. 원인은 `ArchitectureAuditor.audit()`이 정렬(sort)이 끝난 **이후**에 실행되었기 때문. 즉, 분류기 라벨(`TEST_ARTIFACT`)이 정렬기에 전달되지 않은 채 FanOut 점수(206점)로만 정렬되었음.
- **조치 (랭킹)**: `ValidationEngine.ts` 내에서 `ArchitectureAuditor.audit()` 실행 위치를 정렬기(`topCandidates.sort()`) **이전**으로 상향. `architecturalRole`을 정렬 시점 전에 주입받아 페널티 조건(`a.architecturalRole === 'TEST_ARTIFACT'`)이 정상 작동하도록 수정.
- **원인 (노이즈)**: `simulation/fixtures/..` 등 테스트 파일이 여전히 Boundary Evidence(의존성 증거)에 등장. 테스트 자산으로 향하는 엣지들이 전체 아키텍처 Boundary Edge 통계에 합산되고 있었음.
- **조치 (노이즈)**: `ValidationEngine.ts`의 엣지 순회(target accumulation) 로직에 테스트 목적지(`e.target.includes('/test/')`, `fixtures`, `simulation` 등)를 원천 차단하는 `continue` 구문 추가. 테스트 자산은 아키텍처 결함 산정 엣지로 취급되지 않음.
