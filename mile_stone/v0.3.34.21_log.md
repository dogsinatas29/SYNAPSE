# encoding: utf-8
# 📝 Milestone v0.3.34.21 Work Log (작업 기록)

## 📅 Phase 1: Node Role Classification 완료
**상태:** ✅ Completed

### 🛠️ 구현 내용
1. **ASSEMBLY_POINT 구조 분리**
   - `SemanticRole` enum에서 `ASSEMBLY_POINT`를 제거하고, `Node` 인터페이스에 독립적인 `isAssemblyPoint?: boolean` 속성으로 분리.
   - 사유: 특정 노드가 `CORE_RUNTIME`이면서 동시에 `ASSEMBLY_POINT`일 수 있는 다중 상태를 지원하기 위함.

2. **Regex 기반 역할(Role) 필터링 도입**
   - `src/core/GraphAnalyzer.ts`에 경로 기반 정규식 필터링 로직 추가.
   - `TEST`, `SAMPLE`, `BENCHMARK`, `TOOLING`, `DOCUMENTATION`, `GENERATED` 로 노드의 `semanticRole`을 정교하게 분류.
   - 위 조건에 해당하지 않는 일반 파일은 `CORE_RUNTIME`으로 기본 지정.

3. **Runtime Hub(Top Impact Files) 랭킹 Hard Filter 적용**
   - `src/core/validation/ValidationEngine.ts`에서 상위 허브 산출 시 `TEST`, `SAMPLE`, `BENCHMARK`, `TOOLING`, `DOCUMENTATION`, `GENERATED` 노드들을 즉시 필터아웃(Filter out).
   - 효과: `verifier.c`나 `rust_binder_main.rs` 같은 테스트/샘플 코드가 커널 코어 런타임 허브로 둔갑하는 심각한 오탐(False Positive) 원천 차단.
