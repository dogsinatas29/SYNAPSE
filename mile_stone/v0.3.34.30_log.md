# encoding: utf-8
# Milestone v0.3.34.30 - Architecture What-if Laboratory

## 📅 작업 정보
- **상태:** ✅ Completed
- **관련 마일스톤:** v0.3.34.28_29
- **목표:** 기존 분석 엔진들(26, 27, 28, 29)을 기반으로, 새로운 분석 로직(BFS/DFS 등)을 추가하지 않고 순수 오케스트레이션(Orchestration) 계층으로서의 What-if 시나리오 시뮬레이션 환경 구축.

## 🧠 상세 설계 및 로직
- **SimulationSession (Orchestrator)**
  - 원본 그래프의 변경(Hash 불일치) 발생 시 세션을 즉각 Invalidate하여 참조 무결성 확보.
  - 무거운 `TopologyOverlay` 전체를 메모리에 저장하지 않고, `{ type: REMOVE_NODE, nodeId: "A" }` 형태의 `SimulationAction`만을 `undoStack`/`redoStack`에 저장(O(Actions) 메모리 복잡도).
  - 저장소에 기록할 수 있는 경량화된 `ScenarioSnapshot` 생성 지원.
- **ExecutiveReportDiffBuilder (Pure Diff Projection)**
  - 두 개의 `ExecutiveReport` (Baseline vs Scenario)를 받아 단순 수치적 변화량(Delta)만을 계산하여 반환.
  - `delta > 10% 이면 HIGH` 같은 해석적(Interpretative) 판단을 배제하고, 순수 뷰(View) 역할만 수행.
- **IGraphView 인터페이스 추상화 (v0.3.34.30.1)**
  - 26계층(`FailurePropagator`)이 미래의 27계층(`TopologyOverlay`) 구현체를 직접 Import하는 역방향 의존성(Reverse Dependency) 결함 해결.
  - `schema.ts`에 `IGraphView` 인터페이스를 선언하여 26계층은 추상화에만 의존하도록 리팩토링.

## 🛠️ 기술적 변경 사항
- **File Changes:**
  - `[NEW] src/core/simulation/SimulationSession.ts`: 시뮬레이션 세션 오케스트레이터 추가.
  - `[NEW] src/core/simulation/ExecutiveReportDiffBuilder.ts`: 시나리오 리포트 비교 뷰어 추가.
  - `[MODIFY] src/types/schema.ts`: `SimulationAction`, `ScenarioSnapshot`, `ScenarioComparison`, `IGraphView` 타입 추가.
  - `[MODIFY] src/core/FailurePropagator.ts`: `TopologyOverlay` 임포트 제거 및 `IGraphView` 기반 필터링 적용.
  - `[MODIFY] src/core/simulation/TopologyOverlay.ts`: `IGraphView` 인터페이스 구현.
  - `[NEW] scripts/test_architecture_verification_30.ts`: 7가지 불변성 검증 테스트 스크립트 작성.
  - `[NEW] scripts/test_architecture_dependency_audit.ts`: 계층별 DAG 및 역방향 의존성 정적 감사 스크립트 작성.

## ⚠️ 예외 처리 및 주의 사항
- **Layer Integrity:** 
  - `Simulation/*` 내부에서는 `BFS`, `DFS`, `GraphTraversal`, `AuthorityDetector` 등 어떠한 계산 엔진도 임포트할 수 없도록 스크립트로 강제 차단.
- **State Preservation:**
  - 시나리오 비교 객체(`ScenarioComparison`)에 Delta 결과를 캐싱/저장하지 않음. Delta는 오직 호출 시점에 뷰어 함수를 통해 동적으로만 계산됨.

## 📝 Post-Work Log (작업 후 기록)
- 7단계 Invariant Test (Original Graph Integrity, Overlay Leak, Undo/Redo, Scenario Determinism, Kernel Stress, Strict Import, Overlay Replay Determinism) 모두 통과.
- Architecture Dependency Audit 결과 `v0.3.34.24 ~ 30`에 대한 완벽한 DAG(Cycle 0, Reverse Dependency 0) 증명.
- 불필요한 Git 덤프 파일 및 테스트 산출물들(11만 개+) 완전 삭제(Cleanup) 완료.
- **[UI/UX Cleanup]** 작동하지 않는 레거시 UI 요소(Strategy 메뉴, Protocol MD 버튼, AI Scan, Necrosis/Tombstone 시뮬레이션) 비활성화 및 주석 처리.
- **[UI/UX Cleanup]** 3대 보고서(Architecture, Onboarding, Executive) 버튼을 Verify 메뉴의 Simulation Debug 그룹 하위로 통합 재배치.
- **[Architecture Regression Fix]** Virtual Debugger를 What-if Laboratory의 단일 Source of Truth로 복구. 
  - `VirtualDebugger.ts`가 ASR 분석 결과를 `synapse_data/simulation_context.json`으로 영구 저장하도록 파이프라인 추가.
  - `ReportBundleGenerator.ts`에서 가짜 렌더링 템플릿(ValidationContext)을 버리고, `simulation_context.json`을 읽어들여 InsightEngine에 주입하도록 수정. (파일 부재 시 "Please 전 Virtual Debug first" 에러 발생).
  - `InsightEngine.ts` 내 3대 보고서(Executive, Architect, Onboarding) 및 Simulation Insight 추출 로직이 `SimulationContext`(실제 AST 결함, Blast Radius)를 소비하도록 전면 개편.

## 🧠 SYNAPSE Report System Audit & Next Scope (v0.3.34.30 마감 작업)
- **현재 진단:** 
  - Evidence & Finding Engine은 훌륭하게 작동하며 수백 개의 위반 사항(Cycle, Boundary, Layer Gravity 등)을 완벽히 찾아내고 있음. (Level 3.5 - 정적 분석기를 넘어서 주니어 아키텍트 수준 도달)
  - 반면, Report Engine은 595개의 Finding을 그대로 쏟아내거나(Architect), 비어있는 껍데기만 출력하거나(Executive/Onboarding), 잘못된 우선순위를 보여줌.
  - 근본 원인은 발견한 문제를 사람의 역할에 맞게 '압축'하고 '우선순위'를 매기는 엔진(Priority & Compression)이 부재하기 때문.
- **v0.3.34.30 마감 목표 (보고서 엔진 정상화):**
  - 새로운 분석기(Failure Propagation 등) 추가 금지. 오직 기존 595개 Finding의 "정리"에만 집중.
  - **FindingAggregator:** 595개의 Finding을 소스/원인 클러스터 기준으로 27개 등의 Problem Group으로 병합.
  - **PriorityEngine:** 개별 Finding이 아닌 Group을 기준으로 CRITICAL/HIGH/WATCH/IGNORE 우선순위 부여 (Blast Radius 및 위반 집중도 기반).
  - **CompressionEngine:** 최종적으로 수십 개의 그룹을 8대 핵심 위험과 3대 즉시 조치 사항으로 압축.
  - **OnboardingAnalyzer (독립):** 기존 Architect 로직과 분리하여, 순수하게 신규 개발자를 위한 읽기 순서(Entry Point -> Core Pipeline)와 Safe Area를 도출.
- 이 단계가 완료되면 SYNAPSE는 단순한 '문제 발견기'를 넘어, 실제 시니어 아키텍트처럼 '버릴 것은 버리고 핵심만 짚어주는' Level 4 보조 도구로 격상될 것임.

## 🛠️ 보고서 엔진 파이프라인 구현 및 검증 결과
- **Aggregation, Priority, Compression 적용 완료:**
  - `RootCauseAggregator`: 개별 Finding을 근본 원인(Source/Target) 중심으로 `ProblemGroup`으로 그룹핑 성공.
  - `PriorityEngine`: 위반 집중도와 파급력을 기반으로 `CRITICAL`, `HIGH`, `WATCH`, `IGNORE` 판정 알고리즘 탑재 완료.
  - `CompressionEngine`: 595개의 무의미한 나열을 단 5개의 핵심 위험 영역(WATCH)으로 압축하고 460개의 노이즈를 완벽하게 은닉.
- **Presentation Sanitization Layer 발굴 및 적용 (Critical Fix):**
  - 초기 온보딩 및 시뮬레이션 보고서에 `AGGREGATE_OUT_OF_SCOPE` 같은 가상 노드(Synthetic Node)와 `test_analyze.ts` 같은 테스트 파일이 Entry Point로 오염되는 치명적 결함 발견.
  - 이 오염이 단순한 추출 로직 버그가 아니라, **Evidence Graph 자체에 포함된 가상 노드들이 Presentation Layer로 여과 없이 노출되는 아키텍처 수준의 문제**임을 증명.
  - `isRealFile()` 및 `getRoleScore()` 기반의 강력한 필터링(Data Sanitization)을 `InsightEngine`과 `OnboardingAnalyzer`에 전면 적용. 테스트/스크립트 파일엔 -1000점 페널티, 부트스트랩엔 가산점 부여.
- **검증 완료 지표:**
  - `Simulation Debug` 리포트에서 `AGGREGATE_`가 사라지고 `extension -> BootstrapEngine -> DataPipeline -> CanvasPanel`이라는 완벽한 실제 아키텍처 뼈대 검출 성공 (8/10).
  - `Onboarding Report`에서 N/A와 테스트 파일을 걷어내고, 정확히 `src/extension.ts`를 Entry Point로 도출 성공 (7/10).

## ⚠️ Known Limitations (v0.3.34.31 이관 사항)
v0.3.34.30의 목표였던 "Finding Dump → Actionable Insight" 전환은 달성하여 마일스톤을 **COMPLETE** 처리하나, 다음 품질 개선 과제가 남음:
1. **Executive Health Scoring Tuning:**
   - 596개의 Evidence와 다수의 `WATCH` 경고가 존재함에도 Executive Report가 지나치게 낙관적으로 `STABLE`을 선언하는 문제 (임계값 100점 미달로 인한 맹점). 건강도 산정 알고리즘의 민감도 재조정 필요.
2. **Onboarding Core Pipeline Refinement:**
   - 현재 Core Pipeline 추출 로직이 '실행 흐름(Execution Flow)'보다는 '구조적 중심성(Structural Centrality - 주로 모델/타입 참조 빈도)'을 반영하고 있어, 신규 개발자 온보딩 순서로서의 직관성이 다소 떨어짐. 실행 궤적 기반의 파이프라인 도출 로직 강화 필요.
## 🚀 추가 작업 내역 (Godot 스케일 성능 병목 해결)
- **Call Stack Limit (RangeError) 해결:**
  - `ArchitectureIrBuilder.ts`, `ReasoningSnapshot.ts`, `RuleEngine.ts`에서 `push(...array)` 형태의 배열 Spread 연산자가 5만 개 이상의 대규모 그래프(Godot 등)에서 V8 인자 개수 제한을 초과하여 `RangeError: Maximum call stack size exceeded`를 발생시키던 치명적 결함 발견 및 수정. (`for` 루프 단일 `push`로 패치)
- **UI Thread OOM (Silent Crash) 해결:**
  - `Virtual Debug`는 성공하나 UI의 보고서 버튼(`Architect`, `Onboarding` 등) 클릭 시 아무 반응이 없던 원인 규명.
  - `CanvasPanel.ts`의 `handleUnifiedReportGeneration` 진입 시, 20만 개의 엣지를 `map`으로 순회하며 불필요하게 20만 개의 `intentEdges` 객체를 새로 메모리에 할당하던 완벽한 데드 코드(Dead Code)를 적발하여 완전 삭제. (이로 인해 Extension Host가 OOM 또는 틱 타임아웃으로 강제 종료되었음)
- **Report Engine 성능 로깅 인프라 구축:**
  - 신규 보고서 엔진 컴포넌트(`RootCauseAggregator`, `PriorityEngine`, `CompressionEngine`, `ReportBundleGenerator`) 전 구간에 `[REPORT]`, `[AGGREGATOR]` 등 `console.log` 및 `Logger.info` 기반 시작/종료 추적기를 삽입.
  - O(N²) 등 알고리즘 병목이 존재하지 않음을 증명하고, 향후 대규모 렌더링 시 파이프라인의 정확한 지연 구간을 찾아낼 수 있는 기반 마련.

## 🚀 추가 작업 내역 2 (보고서 엔진 Semantic Bug 및 배포 파이프라인 수정)
- **Build Guard 스크립트 수정:**
  - `build-guard.js` 실행 시 `demo` 폴더가 존재하지 않으면 `ENOENT` 복사 에러가 발생하는 문제를 해결하기 위해 폴더 자동 생성(`fs.mkdirSync(..., { recursive: true })`) 로직 추가.
- **Webview UI 상태 초기화 버그(버튼 잠금 현상) 해결:**
  - `ui/index.html`에 하드코딩되어 있던 보고서 버튼 3종(`Architect`, `Executive`, `Onboarding`)의 `disabled` 속성을 완전히 제거.
  - 기존에는 `Virtual Debug`가 완료될 때만 잠금이 해제되었으나, `Developer: Reload Window` 등으로 웹뷰가 새로고침되면 이미 디스크에 증거 파일이 존재함에도 버튼이 잠겨 클릭 이벤트(IPC 메시지)가 서버로 전송되지 않던 치명적 결함을 고침.
- **RootCauseAggregator 대규모 그래프 맵핑 및 그룹화(Grouping) 로직 개편:**
  - **Subsystem Grouping:** Godot과 같은 대형 프로젝트에서 개별 파일(1.6만 개) 단위로 그룹이 쪼개져 1점짜리 노이즈로 취급되는 현상("No high-gravity modules" 오류)을 해결하기 위해, 원인 노드의 파일 경로를 최상단 2계층 폴더(예: `core/config`)로 묶어 거대한 모듈 단위로 스케일을 조정.
  - **Semantic Mapping:** `VirtualDebugger`가 쏟아내는 고유 위반 타입(`necrosis`, `fracture`, `cycle`)을 기존 `PriorityEngine`이 계산할 수 있는 `fanOut`, `boundaryCrossings`, `cycleParticipation` 지표로 정확히 매핑하여 가중치(Threshold)를 돌파할 수 있도록 평가 기준 정상화.

## 🚀 추가 작업 내역 3 (Godot 스케일 외부 의존성 노이즈 차단)
- **RootCauseAggregator EXTERNAL 노이즈 필터링:**
  - 초대형 프로젝트(Godot) 분석 시 `thirdparty/` 등 외부 모듈에서 15,000개 이상의 무의미한 병목(Pressure) 경고가 발생하여 보고서 파이프라인(Aggregator)이 마비되는 문제 해결.
  - `.synapseignore`를 통한 원천 차단 대신, `RootCauseAggregator` 진입 직후 `category === 'EXTERNAL'`인 항목을 `continue`로 건너뛰도록 패치하여 그래프 무결성(Graph Integrity)은 유지하되 보고서 노이즈만 제거하는 아키텍처 원칙 준수.

## 🚀 추가 작업 내역 4 (Boundary 분석 한계 규명 및 리포트 문구 정상화)
- **FractureAnalyzer 원인 규명:**
  - `FractureAnalyzer` 코드를 분석하여, 이 룰이 구조적 의존성(Boundary)을 검사하는 것이 아니라 오직 언어 서버의 컴파일 에러(`context.diagnostics`)에 기반한 `necrosis` 노드의 Outgoing Edge만을 쫓는 로직임을 확인.
  - Godot 분석 시 C++ Language Server 에러가 주입되지 않아 `necrosis`가 0개이므로 `fracture`가 0개가 나오는 것이 정상 설계(By Design)임을 규명함. 
- **Blast Radius 리포트 텍스트 오해 교정:**
  - `InsightEngine`에서 `Risk Area for N violations`라는 텍스트가 실제로는 에러 개수가 아니라 Impact(Blast Radius)의 합산치(N)를 보여주고 있어 오해를 유발함을 확인.
  - 문구를 `[CRITICAL/WATCH] N structural cycles\nBlast Radius: M files` 포맷으로 변경하여 N과 M의 의미를 정확히 분리 및 명시함.

## 🚀 추가 작업 내역 5 (RiskType 의미 계층 분리)
- **RiskClassifier 도입:**
  - `PriorityEngine`이 Cycle(구조적 결함)과 Fan-out(허브 역할)을 단일 스코어로 혼합하여 해석력을 떨어뜨리는 문제를 해결하기 위해, 점수 계산 이전에 의미를 분류하는 `RiskClassifier` 계층 추가.
  - `ProblemGroup`에 `primaryRiskType` 및 `riskTags` 속성 추가 (`STRUCTURAL_DEFECT`, `BOUNDARY_ISSUE`, `ARCHITECTURAL_HUB`, `EXTERNAL_PRESSURE`, `NORMAL`).
- **InsightEngine 연동:**
## 🚀 0.3.34.30 마일스톤 최종 결론 (Godot 검증 기반)
- **Reporting Layer의 근본적 한계와 성과 증명:**
  - `RiskClassifier` 도입 결과, 리포트에 쏟아지던 수많은 노이즈들이 사실 결함(Defect)이 아니라 `ARCHITECTURAL_HUB`로서의 역할(Role)이었음이 명백히 드러남.
  - 리포트에서 측정하지도 않는 `0 boundary violations` 노이즈 문구를 영구 삭제하여 해석의 혼란 방지.
- **Next Step (v0.3.35 Topology 신뢰도 검증):**
  - 새로운 룰(Boundary, Ownership 등)을 무작정 얹기 전에, 현재 시냅스가 찾아낸 **"13개의 순환 참조(Cycle)가 정말로 소스 레벨에서 실존하는가?"**를 샘플링 검증하는 과정이 최우선 과제로 도출됨.
  - 이 검증이 통과되어 Topology 계층의 신뢰도가 100% 입증된 이후에만 Boundary 분석기를 도입하기로 합의함.
