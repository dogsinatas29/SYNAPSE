# v0.3.34.40 - Candidate Discovery Audit

## Status

Planned

## Background

v0.3.34.39 결과, BoundaryDetector 자체는 정상 동작하는 것으로 확인되었다.

발견된 주요 사실:

* `src/core/analysis` → 승격 성공
* `src/core/ir` → 승격 성공
* `src/core/reasoning` → 승격 성공
* Wrapper 판정 정상
* Cohesion 계산 정상

반면 승격 실패 영역들은 대부분 `REJECT_SMALL`로 분류되었다.

대표 사례:

| Candidate              | Members | Internal Edges | Result       |
| ---------------------- | ------- | -------------- | ------------ |
| src/server             | 4       | 0              | REJECT_SMALL |
| src/bootstrap          | 1       | 0              | REJECT_SMALL |
| src/webview            | 1       | 0              | REJECT_SMALL |
| src/core/collaboration | 17      | 14             | REJECT_SMALL |
| src/core/metrology     | 25      | 13             | REJECT_SMALL |

따라서 문제는 Boundary 승격 로직이 아니라 Candidate Discovery 단계에 존재할 가능성이 높다.

---

# Goal

Boundary Candidate가 어떻게 생성되는지 추적하여 실제 subsystem이 왜 축소된 상태로 BoundaryDetector에 전달되는지 확인한다.

---

# Out Of Scope

다음 항목은 이번 버전에서 수행하지 않는다.

* Boundary 기준값 조정
* Cohesion Threshold 조정
* MIN_INTERNAL_EDGES 변경
* Wrapper 로직 수정
* Unknown 감소 작업
* Architect Report 개선
* Onboarding 개선

원인 확인 전 튜닝 금지.

---

# Success Criteria

| ID   | Criterion                                 |
| ---- | ----------------------------------------- |
| SC-1 | src/webview Candidate 생성 경로 확인            |
| SC-2 | src/server Candidate 생성 경로 확인             |
| SC-3 | src/bootstrap Candidate 생성 경로 확인          |
| SC-4 | src/core/collaboration Candidate 생성 경로 확인 |
| SC-5 | Candidate 생성 전/후 노드 수 기록                  |
| SC-6 | Candidate 축소 발생 지점 식별                     |
| SC-7 | Candidate Discovery Pipeline 문서화          |
| SC-8 | tsc --noEmit PASS                         |
| SC-9 | webpack PASS                              |

---

# Investigation Targets

## Target A — src/webview

현재 상태:

```text
Members: 1
Internal Edges: 0
```

확인 필요:

* 실제 GraphModel 노드 수
* Candidate Builder 입력 노드 수
* Candidate Builder 출력 노드 수

---

## Target B — src/server

현재 상태:

```text
Members: 4
Internal Edges: 0
```

확인 필요:

* 서버 계층 노드 누락 여부
* Prefix Grouping 과정
* Hierarchical Candidate 생성 과정

---

## Target C — src/bootstrap

현재 상태:

```text
Members: 1
```

확인 필요:

* 단일 파일 subsystem으로 인식되는 이유
* 부모 Prefix 제거 여부

---

## Target D — src/core/collaboration

현재 상태:

```text
Members: 17
Internal Edges: 14
```

확인 필요:

* 실제 후보 크기
* Internal Edge 계산 방식
* MIN_INTERNAL_EDGES 적용 전 상태

---

# Phase 1

## Candidate Discovery Trace

Candidate 생성 파이프라인 전체 추적.

기록:

```text
Prefix
↓
Raw Members
↓
Filtered Members
↓
Candidate Members
↓
Boundary Input
```

---

# Phase 2

## Node Loss Audit

각 단계에서 제거되는 노드 기록.

예시:

```text
src/webview

Raw: 42
Filtered: 18
Candidate: 1
Boundary: 1
```

---

# Phase 3

## Root Cause Classification

발견된 원인을 아래 범주로 분류.

* PREFIX_COLLAPSE
* NODE_FILTERING
* GROUPING_ERROR
* EDGE_CALCULATION
* CANDIDATE_TRUNCATION
* EXPECTED_BEHAVIOR

---

# Deliverables

생성 결과:

* CandidateDiscoveryAudit.md
* CandidateTraceReport.md
* RootCauseClassification.md

---

# Exit Condition

다음 질문에 답할 수 있어야 한다.

> 왜 src/webview가 Members=1 인가?

> 왜 src/server가 Members=4 인가?

> 왜 src/core/collaboration이 Internal Edges 14로 계산되는가?

이 세 질문에 증거 기반 답변이 가능해지면 v0.3.34.40 완료.

