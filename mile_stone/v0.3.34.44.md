# encoding: utf-8
# 🚀 Milestone v0.3.34.44 - SYNAPSE Diagnostic Foundation 구축

## 📅 작업 정보
- **상태:** 🚧 In-Progress
- **관련 마일스톤:** v0.3.34.43 (Evidence 투명성 확보 및 파이프라인 수치 보정)
- **목표:** 
  - SYNAPSE가 현재 무엇을 알고 있으며(Pattern), 그 지식을 어떤 개념(Vocabulary)으로 표현할 수 있고, 어떤 질문(Question)에 답할 수 있는지를 정의하는 진단 언어 체계(Diagnostic Language System)를 구축한다.
  - 실제 구현 순서는 `Evidence ➡️ Pattern ➡️ Vocabulary ➡️ Question` 의 상향식(Bottom-Up) 구조를 따른다.

---

## 🛑 핵심 원칙 (Core Principles)

### 원칙 1. 패턴은 삭제하지 않는다 (No Deletion)
- `Authority Reach`, `Blast Radius`, `Control Score` 등이 중복처럼 보여도 임의로 제거하지 않는다. 우선 `Candidate Group`으로 묶어 실체를 규명한다.

### 원칙 2. Pattern Inventory는 '사실'만 적는다 (Objective Only)
- 주관적 판단을 배제하고, 오직 기계적 사실(비용, 저장소, 누수 현황)만 기록한다.

### 원칙 3. Vocabulary는 30개로 제한한다 (Finite Terms)
- 무한정 늘리지 않고, 아키텍트들이 실무에서 즉시 식별 가능한 30여 개의 핵심 용어만 고정한다.

### 원칙 4. 사용자 확장은 오직 Question Layer에서만 허용한다
- **Question (사용자 확장 가능):** 도메인 특화 질문 추가 허용 (`"결제 시스템의 핵심은 어디인가?"`)
- **Vocabulary (고정):** 아키텍처 용어(Domain Core 등)는 엔진 계약으로 보호되며 수정 불가.
- **Pattern (고정):** 패턴의 정의와 산식은 SYNAPSE Core가 통제하며 수정 불가.
- **Evidence (고정):** 실제 추출되는 원시 데이터는 수정 불가.
- ➡️ *이 구조를 통해 Pattern Explosion과 Vocabulary Drift를 방지하고 진단 무결성을 지킨다.*

---

## 🛠️ 단계별 산출물 우선순위

### 1순위: Pattern Inventory (`v0.3.34.44_pattern_inventory.md`)
- **질문:** SYNAPSE는 무엇을 알고 있는가?
- **목표:** SYNAPSE 코드베이스에 존재하는 실제 Pattern 전수조사. 계산되나 버려지는 지식 누수를 찾는다.
- **필수 컬럼:** `Pattern`, `Layer`, `Language Independent`, `Source`, `Generation Time`, `Storage`, `In Graph?`, `Persisted?`, `Computed but Dropped?`, `Used in Report?`, `Renderer Impact`, `Memory Impact`, `Bootstrap Impact`, `Vocabulary Candidate?`

### 2순위: Pattern Lineage (`v0.3.34.44_pattern_lineage.md`)
- **질문:** 이 패턴은 어디서 오는가?
- **목표:** 패턴 간의 계보도 생성 및 누수(Leakage) 지점 추적.

### 3순위: Pattern Survival Audit (`v0.3.34.44_pattern_survival.md`) - 🌟 NEW Phase 2.5
- **질문:** 이 버려지는 패턴을 정말 영구 보존(Persist)해야 하는가?
- **목표:** JSON 비대화를 막기 위해 패턴별로 `KEEP`, `ALIAS`, `DERIVED`, `DROP CANDIDATE` 판정.

### 4순위: Vocabulary (`v0.3.34.44_vocabulary.md`)
- **질문:** 아키텍트는 어떤 언어로 생각하는가?
- **목표:** Survival Audit을 통과한 순수 패턴들을 재료로 삼아 30개의 핵심 용어 선별.

### 5순위: Contract (`v0.3.34.44_contract.md`)
- **질문:** 용어는 어떤 패턴으로 정의되는가?
- **목표:** `Vocabulary = Pattern 조합` 의 맵핑 공식 확립. 왜 저장해야 하는가에 대한 계약적 증명.

### 6순위: Vocabulary Dependency Audit (`v0.3.34.44_vocabulary_audit.md`) - 🌟 NEW Phase 4.5
- **질문:** 이 용어들은 정말로 독립적인 개념(Archetype)인가?
- **목표:** Contract에서 증명된 용어들이 사실상 동일한 패턴 조합의 포장지(Alias)에 불과한지 교차 검증하고, 진단 언어의 순도를 높인다.

### 7순위: Pattern Coverage Audit (`v0.3.34.44_coverage_audit.md`)
- **질문:** 30개의 종합 아키텍처 진단 용어를 완성하기 위해 최소 몇 개의 패턴이 더 필요한가?
- **목표:** SYNAPSE가 현재 '의존성 진단기'에 불과함을 인정하고, 부족한 패턴(Missing Pattern Catalog)을 발굴한다.

### 8순위: Pattern Expansion Roadmap (`v0.3.34.44_pattern_roadmap.md`)
- **질문:** SYNAPSE가 진정한 30대 Vocabulary를 구사하기 위해 앞으로 어떤 Pattern Family가 추가되어야 하는가?
- **목표:** 결핍된 패턴 패밀리(Ownership, Churn 등)에 대한 스캐너 확장 로드맵을 작성하고, Persistence 대상 패턴들을 Tier(즉시/향후/계산식)로 분류하여 영구 저장소 전략의 거시적 기반을 다진다.

### 9순위: Pattern Dependency Stress Test (`v0.3.34.44_pattern_stress_test.md`)
- **질문:** Tier 1으로 분류된 패턴이 제거된다면 진단 언어가 얼마나 붕괴하는가? 다른 패턴으로 대체 가능한가?
- **목표:** '필수 패턴'이라 여겨진 것들이 정말 대체 불가능(Irreplaceable)한지 스트레스 테스트를 거쳐, Persistence Layer에 저장될 자격을 최종적으로 반증(Falsification)한다.

### 10순위: Persistence Audit (`v0.3.34.44_persistence_audit.md`)
- **질문:** 어떤 형태로 저장해야 숫자(Value)가 아닌 근거(Evidence)와 패턴(Pattern)을 동시에 보존할 수 있는가?
- **목표:** 단순한 구현 설계가 아닌 감사(Audit)의 관점으로 접근하여, 생성 비용(Cost), 무효화 민감도(Invalidation), 저장 형태(Storage Shape)를 평가하고 진정한 'SYNAPSE Knowledge Model'을 확립한다.

### 11순위: Knowledge vs Cache Audit (`v0.3.34.44_knowledge_audit.md`) - 🌟 NEW Phase 6.5
- **질문:** 우리가 영구 보존하려는 데이터는 잃어버리면 복구 불가능한 진짜 '지식(Knowledge)'인가, 아니면 언제든 재계산 가능한 '캐시(Cache)'인가?
- **목표:** SYNAPSE의 저장소가 성능 최적화를 위한 단순 캐시 저장소로 전락하는 것을 막고, 사용자 정의 룰(Rule)이나 의미(Vocabulary) 같은 진정한 Source of Truth만을 가려낸다.

### 12순위: Question Catalog (HOLD)
- **질문:** 사용자는 무엇을 묻는가?
- **상태:** 보류. (언어 체계와 결손 패턴 발굴이 완료된 이후 확장)

---

## 📝 맺음말
0.3.34.44의 진짜 성과는 "질문 시스템 구축"이 아니라, **"SYNAPSE가 가진 모든 지식을 처음으로 인벤토리화하는 것"**이다. 이 기반이 완료되어야 이후 `core_patterns.json`, `user_questions.json` 확장이 안전하게 열린다.
