# 🚀 Milestone v0.3.34.54 - Simulation Evidence Contract Reconciliation (Track B)
## 📅 작업 정보
- **상태:** 🚧 In-Progress
- **관련 마일스톤:** v0.3.34.53
- **목표:** `.49` Evidence Schema를 Contract로 복원하고, 경로 A(Evidence Layer 구현)의 진입 조건 확정

## 🧠 Root Cause (FACT - Level 4)

`.49`에서 정의·사용된 Evidence schema(`type:CASCADE`, `affectedNodeCount`, `propagationDepth`)를 생성하여 `evidenceBundle.findings`에 공급하는 **현재 실행 경로가 구현되어 있지 않음.**
(`SimulationEvidenceCollector` 등 관련 코드는 존재하나, 이 schema를 생성하는 경로가 없음)

### 계층 구조
```text
[.49 설계 의도]
  Metric
    ↓
  Evidence Layer  ← 현재 Gap (생성 경로 미구현)
    ↓
  Pattern Detector  ← 존재 (v0.3.34.45부터 동일)
    ↓
  Report

[경로 C 방식 — 채택 불가]
  Metric → AAE raw findings → Detector  ← .49 계층 우회
```

## 🛠️ 8단계 구현 계획 (경로 A)

| Step | 내용 | 상태 |
|---|---|---|
| 1 | `.49` Evidence 샘플 → 최소 schema 추출 | ✅ **FACT** |
| 2 | `PROPAGATION` / `CASCADE` 의미 정의 | ✅ **FACT** |
| 3 | Metric → Evidence 변환 담당 원체(Source) 확인 | 📌 **책임 후보 확정** (Orchestration 위치 미결 → `.55`) |
| 4 | `SimulationEvidenceType`과의 관계 파악 | 🔵 **LIKELY** (통합 요구 없음, `.55`에서 확정) |
| 5 | 최소 fixture 설계 | ⏳ 대기 → `.55` |
| 6 | Evidence Generator 구현 | ⏳ 대기 → `.55` |
| 7 | Detector 입력 확인 (`[DT-B2]` 로그) | ⏳ 대기 → `.55` |
| 8 | `SIMULATION_DEBUG` 최종 렌더링 검증 | ⏳ 대기 → `.55` |

## 🔎 Step 1 결과 (FACT)

**`.49` Evidence 최소 schema** (`v0.3.34.49_pipeline.md` L412~419)
```json
{
  "type": "CASCADE",
  "affectedNodeCount": 18,
  "propagationDepth": 4
}
```

**Detector 소비 필드** (`CascadeFailureDetector.ts`, v45~v49~현재 동일)
```ts
f.type === 'simulation' || f.evidenceType === 'PROPAGATION' || f.evidenceType === 'CASCADE'
f.metadata?.affectedNodeCount
f.metadata?.propagationDepth
f.metadata?.cascadeImpact  // Schema fallback
f.targetId || f.nodeId
```

## ⚠️ 주의 사항
- 경로 B 제외 확정 (FailurePropagationReport → findings 변환은 새 의미론)
- 경로 C 채택 불가 (`.49` Evidence 계층 우회)
- Step 2~4 완료 전 Generator 구현 착수 금지

## 📝 Post-Work Log
- Step 1~2: FACT 수준 완료
- Step 3: Metric 원체(`FailurePropagator`) 확인. **Orchestration 위치는 `.55`에서 결정**
- Step 4: `SimulationEvidenceType` 통합 요구 없음 (LIKELY). **`.55`에서 확정**
- `.54` Contract Reconciliation 단계 종료. `.55` 첫 작업 = Orchestration 위치 확정 후 Generator 구현.

## 🔎 Step 2~4 핵심 결론

| 항목 | 판정 | 수준 |
|---|---|---|
| `CASCADE` = `FailurePropagator.cascadeImpact` (BFS depth≥3) | FACT | Level 2~3 |
| `PROPAGATION` 독립 schema 미확인 — Detector guard 조건 | FACT | Level 3 |
| `FailurePropagator`가 `cascadeImpact` 계산의 **원체** | FACT | Level 2 |
| `VirtualDebugger`가 현재 `FailurePropagator`를 호출하지 않음 | FACT | Level 2 |
| Evidence Layer 책임자 = `FailurePropagator` | **미확정** — 원체(Metric source)와 책임자는 별개 | 설계 결정 후보 |
| `StateAuditPipeline → evidenceBundle.findings` 연결 | **미확정** — Orchestration 위치 미결 | 설계 결정 후보 |
| `SimulationEvidenceType` 버폄 독립 | 현재 증거 범위에서 통합 요구 없음 | **지지 (LIKELY)**, `.55`에서 확정 |

## 🚀 `.55` 진입 조건

**코딩 전 쪼. Contract 및 Orchestration 위치 확정 먼저.**

```text
FailurePropagationReport
        ↓
   [??? 설계 결정 필요]
        ↓
 Pattern Evidence (type:'CASCADE', metadata...)
        ↓
 evidenceBundle.findings
        ↓
 CascadeFailureDetector
```

Orchestration 후보 3가지 (코드 구조와 lifecycle 확인 후 선택):
- **A.** `VirtualDebugger`에서 `StateAuditPipeline` 직접 실행 → Evidence Adapter
- **B.** 상위 orchestration layer에서 두 파이프라인 결과를 함께 전달
- **C.** 기존 Simulation 계층에 Evidence Generator를 두고 VirtualDebugger가 소비
