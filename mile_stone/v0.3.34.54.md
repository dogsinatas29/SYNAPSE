# 🚀 Milestone v0.3.34.54 - Simulation Evidence Generator Recovery (Track B)
## 📅 작업 정보
- **상태:** 🏗️ Planned
- **관련 마일스톤:** v0.3.34.53
- **목표:** `SIMULATION/PROPAGATION/CASCADE` Evidence 0건의 근본 원인 확정 및 복구 전략 결정

## 🧠 Root Cause (FACT - Level 4, Semantic Evidence)

### 설계상 의도 (v0.3.34.41 SIMULATION_ARCHITECTURE.md)
```text
project_state.json
  → SimulationProjectionBuilder
  → Scenario/Event/State Transition/Propagation Engine
  → simulation_evidence.json (SIMULATION/PROPAGATION/CASCADE 타입 기록)
  → SIMULATION_DEBUG.md
```

### 현재 실제 경로 (VirtualDebugger.ts)
```text
project_state.json
  → ArchitectureAnalysisEngine (AAE)
    → necrosis/fracture/semantic/cycle/boundary/pressure/schema/structural
  → simulation_evidence.json  ← 설계상 Simulation 파이프라인 결과물이어야 하는 자리
  → SIMULATION_DEBUG.md
```

### 판정
- `FailurePropagator`는 원래 발생원이 아님 — HYPOTHESIS 반증 완료.
- 발생원은 `.41` 설계의 `SimulationProjectionBuilder` + `Propagation Engine` 체인.
- 이 체인은 `src/core/simulation/` 하위에 일부 구현되어 있으나,
  **`VirtualDebugger`에 연결되지 않은 상태** (파이프라인 단절).

## 🛠️ 구현 전 확인 사항 (승인 필요)

**Step 1:** `src/core/simulation/` 내 구현 현황 확인
- `SimulationRuleEngine`, `TopologyMutator`, `SimulationSnapshot` 등의 실제 구현 상태
- `SIMULATION/PROPAGATION/CASCADE` finding을 생성하는 코드가 존재하는가?

**Step 2:** 복구 전략 결정 (확인 후 선택)
- **경로 A (복구):** 기존 Simulation 파이프라인을 VirtualDebugger에 연결
- **경로 B (신규 설계):** AAE findings 기반으로 SimulationRuleEngine을 통해 새로운 finding 생성

**Step 3:** `[DT-B2]` 로그 설계
```text
simulationSeeds=?
propagationImpacts=?
generatedSimFindings=?
generatedPropFindings=?
generatedCascadeFindings=?
bundleTotal=?
detectorInput=?
detectorOutput=?
```

## ⚠️ 주의 사항
- `src/core/simulation/`의 기존 코드를 확인 없이 수정 금지
- `evidenceBundle.findings`에 신규 타입을 추가할 경우 AAE의 다른 소비자(UI, 기존 분석)에 영향 없는지 확인

## 📝 Post-Work Log
- 작업 후 기록 예정

