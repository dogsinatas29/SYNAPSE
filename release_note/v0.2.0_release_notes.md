# Release Notes - v0.2.0 (Milestone: Strategic Evolution)

## [English]
### 🏁 Strategic Execution Flow (SSOT)
- **Single Source of Truth**: Flow View is now the authoritative state for architecture, resolving synchronization drift between different editing modes.
- **START/END Markers**: Terminal nodes added for clear execution boundaries.
- **Functional Entry Point Prioritization**: Automatically prioritizes `main.*`, `index.*`, or `app.*` over metadata files.
- **Ghost Node Isolation**: Disconnected nodes are isolated in a "Storage" cluster for visual clarity.

### 🏗️ Full TypeScript & Logic Support
- **Robust Declaration Scanning**: Full support for `interface`, `type`, and `enum` parsing.
- **Advanced Modifier Detection**: Correctly identifies `public`, `private`, `protected`, and `async` method combinations.
- **Logic Flow Recognition**: `try/catch` blocks and `switch` statements are now visualized as discrete steps.
- **Type-Safe Dependencies**: Accurate mapping of `import type` relationships.

---

## [한국어]
### 🏁 전략적 실행 흐름 (Strategic Execution Flow - SSOT)
- **단일 진실 공급원(SSOT)**: 순서도(Flow View)를 아키텍처의 최종 권위 상태로 정의하여, 다양한 편집 모드 간의 동기화 불일치 문제를 원천적으로 해결했습니다.
- **START/END 마커**: 로직의 시작과 끝을 명시하는 터미널 노드를 추가하여 실행 경로의 완결성을 높였습니다.
- **기능적 진입점 우선순위**: `Cargo.toml`, `GEMINI.md` 등 메타데이터보다 `main.*`, `index.*` 등 실제 실행 루트를 최우선으로 탐색합니다.
- **고스트 노드 격리**: 의존성이 없는 노드들을 'Storage' 클러스터로 자동 격리하여 메인 뷰의 가독성을 확보했습니다.

### 🏗️ 완벽한 TypeScript 및 로직 지원
- **선언부 스캐닝**: `interface`, `type`, `enum`을 완벽하게 인식하여 아키텍처 상의 정규 노드로 취급합니다.
- **고급 수식어 감지**: `public`, `private`, `protected` 및 `async` 메서드의 조합을 정확하게 식별합니다.
- **로직 흐름 고도화**: `try/catch` 예외 처리 구간과 `switch` 분기문을 순서도 상의 개별 스텝으로 시각화합니다.
- **타입 안정적 의존성**: `import type` 관계를 정확히 추적하여 불필요한 실행 오버헤드 없이 아키텍처 관계만 매핑합니다.
