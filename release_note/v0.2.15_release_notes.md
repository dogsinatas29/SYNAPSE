# SYNAPSE Release Notes - v0.2.15

## [v0.2.15] - 2026-02-28
### War Room: Legacy Logic Fix & Flow Precision
**"Every edge must point to the truth of the code."**
**"모든 엣지는 코드의 진실을 가리켜야 한다."**

---

### 🚀 주요 변경 사항 (Key Changes)

#### 1. Flow Scanner 리팩토링 (Flow Logic Refactoring)
- **[KR]** 비선형 흐름 지원: `if/else`, `while`, `for`, `try/except` 등의 제어문을 인식하여 실제 논리 구조를 반영하는 순서도를 생성합니다.
- **[EN]** Non-linear Flow Support: Processes control statements like `if/else`, `while`, `for`, and `try/except` to generate flowcharts that represent actual logical structures.
- **[KR]** Stack-based Analysis: 구문 수준 스캐닝을 통해 블록의 시작과 끝을 파악하고 정밀하게 엣지를 연결합니다.
- **[EN]** Stack-based Analysis: Identifies block boundaries through syntax-level scanning for precise edge connections.

#### 2. 성능 및 가시성 최적화 (Performance & Visibility Optimization)
- **[KR]** 좌표 드리프트 해결: `renderNode`의 `save/restore` 불균형을 수정하여 노드 라벨과 아이콘이 줌 레벨에 상관없이 선명하게 표시됩니다.
- **[EN]** Coordinate Drift Fix: Resolved `save/restore` imbalance in `renderNode`, ensuring labels and icons remain sharp and correctly positioned at any zoom level.
- **[KR]** CPU 점유율 최적화: 무거운 그림자 효과를 '활성 경로(Active Path)'에만 제한하여 렌더링 부하를 대폭 낮췄습니다.
- **[EN]** CPU Optimization: Limited expensive shadow effects to the 'Active Path' only, significantly reducing rendering overhead.
- **[KR]** 모드별 호버 지원: Flow 모드와 Tree 모드에서도 마우스 호버를 통한 경로 하이라이팅이 가능해졌습니다.
- **[EN]** Mode-specific Hover: Enabled path highlighting via mouse hover in both Flow and Tree modes.

#### 3. UX 및 로직 시각화 (UX & Logic Visualization)
- **[KR]** Bypass 로직 시각화: 조건에 따른 우회 경로가 이제 순서도 상에서 정확하게 표현됩니다.
- **[EN]** Bypass Logic Visualization: Conditional bypass paths are now accurately represented in the flowchart.
- **[KR]** Semantic Zooming (LOD): 줌 레벨에 따라 정보의 밀도를 조절하여 인지 부하를 최소화합니다.
- **[EN]** Semantic Zooming (LOD): Minimum cognitive load by adjusting information density based on zoom level.

---

### 📦 설치 방법 (Installation)
```bash
code --install-extension synapse-visual-architecture-v0.2.15.vsix
```

### 🎖️ 작전 지휘 (Command)
**Gemini Strategic Support AI**
"로직의 미궁을 시각적으로 해소하는 것, 그것이 SYNAPSE의 사명입니다."
"To visually resolve the labyrinth of logic—that is the mission of SYNAPSE."

[Status: Verified by Commander Dogsinatas]
