# 🚀 SYNAPSE v0.2.1 Release Notes

## 🇺🇸 English

### [New Features]
*   **🗑️ Dedicated Delete UI**: A new "Delete" button has been added to the toolbar. The right-click context menu now dynamically shows labels like "Delete Node", "Delete Edge", or "Delete 3 Nodes" based on your selection.
*   **⚠️ Deletion Guard**: A confirmation dialog now appears when attempting to delete multiple nodes to prevent accidental data loss.

### [Enhancements]
*   **➡️ Flow Analysis Engine (v0.2.0+)**:
    *   Significant improvements to TypeScript analysis (async, private, static members, and complex type definitions are now correctly identified).
    *   Analysis depth increased from 8 to 50 steps for comprehensive logic tracing.
    *   **Horizontal Branching**: Logical decisions (if/switch) now branch out horizontally, making complex flows much easier to follow.
*   **🧹 Git Hygiene**:
    *   Updated `.gitignore` to exclude build artifacts, root `GEMINI.md`, and all `.vsix` files.
    *   Optimized repository state by untracking unnecessary files while preserving the `demo/` directory.

---

## 🇰🇷 한국어

### [신규 기능]
*   **🗑️ 전용 삭제 UI**: 툴바에 삭제 버튼이 새롭게 추가되었습니다. 또한 우클릭 컨텍스트 메뉴가 선택 상태에 따라 "노드 삭제", "엣지 삭제", "3개의 노드 삭제" 등으로 스마트하게 변합니다.
*   **⚠️ 삭제 보호**: 여러 개의 노드를 한 번에 지울 때 실수로 인한 데이터 손실을 방지하기 위한 확인 창이 추가되었습니다.

### [기능 개선]
*   **➡️ 순서도 분석 엔진 (Flow View)**:
    *   TypeScript 분석 능력이 대폭 향상되었습니다 (비동기 함수, 클래스 멤버, 복잡한 타입 정의 등을 정확히 포착).
    *   분석 깊이를 8단계에서 50단계로 확장하여 더 넓은 범위의 로직을 추적합니다.
    *   **수평 분기 레이아웃**: 조건문(if/switch) 발생 시 대체 경로가 옆으로 뻗어나가도록 개선되어 복잡한 로직도 가독성 있게 표현됩니다.
*   **🧹 Git 저장소 최적화**:
    *   `.gitignore` 설정을 강화하여 루트의 `GEMINI.md`, 모든 `.vsix` 파일, 빌드 임시 파일들을 관리 대상에서 제외했습니다.
    *   불필요한 파일들의 추적을 중지하여 저장소를 깨끗하게 유지하면서도, `demo/` 폴더는 요청대로 유지했습니다.
