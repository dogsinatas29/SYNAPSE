# encoding: utf-8
# 🚀 Release Notes - v0.2.53 "The Mainframe Breach: Return Path Secured"

## 🇰🇷 한글 요약 (Korean Summary)

### 🛡️ 핵심 변경 사항: Main World(Context 1) 정밀 요격 & 브릿지 탈취
이전 버전(v0.2.52)에서 Isolated World에 갇혀 데이터 귀환 경로(Return Path)가 차단되었던 문제를 해결했습니다. 이제 CDP를 통해 실제 앱이 구동되는 Main World를 직접 식별하여 주입합니다.

1. **Main World (Context 1) 자동 식별 & 침투**: `Runtime.executionContextCreated` 이벤트를 분석하여 Isolated World가 아닌 실제 앱 실행 영역에 고스트 스크립트를 우선 주입합니다.
2. **VsCodeApi Hijacking**: `window.acquireVsCodeApi`를 즉시 후킹하여 모든 `postMessage` 트래픽을 완벽하게 가로챕니다.
3. **가시성 강화**: DevTools 콘솔 및 본 확장 프로그램의 출력 채널을 통해 주입 성공 여부와 데이터 인입을 실시간으로 확인 가능합니다.

---

## 🇺🇸 English Summary

### 🛡️ Key Changes: Main World (Context 1) Precision Interception & Bridge Hijacking
Resolved the "Return Path" failure where the injection was trapped in the Isolated World in v0.2.52. We now use CDP to explicitly identify and target the "Main World" where the actual application logic resides.

1. **Main World (Context 1) Auto-Identification**: Analyzes `Runtime.executionContextCreated` events to bypass Isolated Worlds (`vscode-isolated-xxx`) and target the primary execution context.
2. **VsCodeApi Hijacking**: Wraps `window.acquireVsCodeApi` pre-emptively to intercept all `postMessage` traffic between the webview and the host.
3. **Enhanced Visibility**: Visualizes success via the "Injection Beacon" in DevTools and provides clear "Neuro-Hit" labels in the extension output channel.

---

## 🛠️ 수정 파일 (Modified Files)
- `package.json`: v0.2.53
- `src/core/CDPManager.ts`: Main World identification logic.
- `src/core/WebviewInterceptor.ts`: Advanced VSCode API bridge hijacking script.
- `src/core/PromptLogger.ts`: Log header updated to v0.2.53.

## 🚀 검증 (Validation)
1. Check Output Channel: `[SYNAPSE][CDP] Target Context identified: 1`.
2. Check DevTools: `[SYNAPSE][GHOST][DEBUG] Ghost Protocol Injected. Context: MAIN`.
3. Interaction Test: Verify `[SYNAPSE][NEURO_HIT]` data appears on webview actions.

