# 🚀 SYNAPSE v0.2.6 Release Notes

> **"Context is King"**

Version 0.2.6 introduces a major upgrade to the **Log Prompt** workflow, making it a powerful tool for capturing the "Why" behind your architecture.

## 🌟 New Features (기능 추가)

### 🧠 Context-Aware Logging (맥락 인식형 로깅)
The `Log Prompt` (`Ctrl+Alt+M`) feature is now smarter and vastly more useful.
(`Ctrl+Alt+M` 로그 프롬프트 기능이 훨씬 더 똑똑해졌습니다.)

1.  **Node Auto-Binding (노드 자동 바인딩)**
    -   **English**: If you select a node on the canvas and trigger the log, the input context automatically focuses on that node. No need to type "I'm working on Node X..." anymore.
    -   **Korean**: 캔버스에서 노드를 선택하고 로그를 실행하면, 입력 컨텍스트가 자동으로 해당 노드에 맞춰집니다. "Node X 작업 중..."이라고 일일이 칠 필요가 없습니다.

2.  **Visual Tagging (시각적 태깅)**
    -   **English**: Categorize your thoughts instantly.
        -   `🔍 [Discovery]`: Exploration & Research
        -   `🧠 [Reasoning]`: Design Decisions
        -   `⚡ [Action]`: Implementation Steps (Refactoring, etc.)
        -   `🐛 [Fix]`: Bug Resolution
    -   **Korean**: 생각의 종류를 즉시 분류하세요. (탐색, 추론, 실행, 버그수정 등)

3.  **State Snapshotting (상태 스냅샷)**
    -   **English**: When you save a log to `context.md`, SYNAPSE secretly embeds the **current canvas view** (Zoom level, Pan position). This lays the groundwork for a future "Time Travel" feature to restore your exact viewpoint.
    -   **Korean**: `context.md`에 로그를 저장할 때, 현재 **캔버스 뷰 상태**(줌 레벨, 위치)가 비밀리에 함께 저장됩니다. 이는 미래의 "타임 트래블" 기능(뷰 복원)을 위한 초석입니다.

4.  **One-Click Access (원클릭 접근)**
    -   **English**: After logging, click `[Open context.md]` in the notification to immediately review your entry.
    -   **Korean**: 로그 저장 후 알림창의 `[Open context.md]` 버튼을 눌러 기록된 내용을 즉시 확인할 수 있습니다.

## 📄 Documentation (문서화)
-   **English**: Updated `README.md` with a guide on using the new Context UI.
-   **Korean**: `README.ko.md`에 새로운 맥락 UI 사용법 가이드를 추가했습니다.

## 🐛 Bug Fixes & Improvements
-   **UX**: Improved placeholder text dynamically based on selection.
-   **Stability**: Handled edge cases where `projectRoot` might be undefined during logging.
