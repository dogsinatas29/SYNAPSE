# Release Notes - v0.2.10 "Engine Compatibility"

## 🇺🇸 English
Version 0.2.10 addresses a critical activation issue. We've updated the VS Code engine requirement to match our dependencies, resolving the "command not found" error.

### 🐛 Bug Fixes
- **Activation Failure**: Raised the minimum required VS Code version to `^1.82.0` to ensure compatibility with `vscode-languageclient` v9. This fixes the silent crash and "command not found" error during extension startup.

---

## 🇰🇷 Korean
버전 0.2.10은 중요한 활성화 문제를 해결합니다. 의존성 라이브러리와의 호환성을 위해 VS Code 엔진 요구 사항을 업데이트하여 "command not found" 오류를 수정했습니다.

### 🐛 버그 수정
- **활성화 실패**: `vscode-languageclient` v9와의 호환성을 보장하기 위해 최소 VS Code 버전을 `^1.82.0`으로 상향 조정했습니다. 이로 인해 확장 프로그램 시작 시 발생하는 충돌 및 "command not found" 오류가 해결되었습니다.
