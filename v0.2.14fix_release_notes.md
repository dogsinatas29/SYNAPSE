# 🚀 SYNAPSE v0.2.14fix: The Clarity Update (Flow View Overhaul)

This hotfix focuses on completely revamping the Flow View to transform it from a chaotic "globe" of spaghetti into a truly professional, architectural diagram. We enforced strict visual rules to minimize cognitive load and eliminate context loss.
이번 핫픽스는 Flow(순서도) 뷰를 완전히 뜯어고쳐, 스파게티처럼 얽혀있던 "지구본" 현상을 진정한 전문가용 아키텍처 다이어그램으로 탈바꿈시켰습니다. 엄격한 시각적 규칙을 강제하여 인지 부하를 줄이고 컨텍스트 유실을 원천 차단했습니다.

---

## 🛠️ Major Improvements (주요 개선 사항)

### 1. Group-Aware Hierarchy (논리적 클러스터 보존)
- **EN:** **Context Loss Fixed**: Previously, nodes in the Flow View lost their group associations defined in the Graph View. Now, `FlowchartGenerator` and the rendering engine persist `cluster_id` data.
- **KR:** **컨텍스트 유실 문제 해결**: 이전에는 Graph 모드에서 예쁘게 묶여 있던 모듈들이 Flow 뷰로 넘어오면서 개별 노드로 흩어지는 문제가 있었습니다. 이제 `FlowchartGenerator`와 렌더링 엔진이 `cluster_id` 속성을 유지하여 원래의 그룹 논리를 그대로 가져옵니다.
  
- **EN:** **Visual Bounding Boxes**: Nodes belonging to the same cluster (e.g., Input modules, Validation modules) are now visually enclosed in a dashed bounding box (e.g., `[ INPUT MODULES ]`). This prevents related logic from scattering across the canvas.
- **KR:** **시각적 바운딩 박스**: 동일한 클러스터에 속하는 노드들(예: Input 계열, Valid 계열)은 이제 점선으로 된 바운딩 박스(`[ XXXX MODULES ]`) 안에 논리적으로 갇혀 렌더링됩니다. 이로써 관련된 로직들이 캔버스 전체에 무분별하게 흩어지는 현상을 방지합니다.

### 2. Orthogonal Edge Routing (맨해튼 라우팅 & 회피 기동)
- **EN:** **Spaghetti Eradicated**: Replaced the direct, diagonal Bezier curves with 90-degree Manhattan routing. 
- **KR:** **스파게티 박멸**: 시각적 노이즈가 심했던 직선/대각선 베지어 곡선을 90도로 직각으로 꺾이는 맨해튼(Orthogonal) 라우팅으로 교체했습니다.

- **EN:** **Collision Avoidance**: Edges that need to bypass multiple layers now route outwardly around the main logic blocks, explicitly avoiding piercing through unrelated central nodes like `database.py`.
- **KR:** **충돌 회피 알고리즘**: 레이어 단계를 여러 개 건너뛰는 엣지들은 중앙의 주요 로직 블록(예: `database.py`, `calculator.py`)을 무차별적으로 관통하지 않고 캔버스 바깥쪽으로 크게 우회(Bypass)하도록 라우팅 경로를 계산합니다.

### 3. Virtual End-Point Bus (가상 버스 터미널)
- **EN:** **Edge Congestion Solved**: Introduced a virtual `Merge / Sync` node just above the `END` block.
- **KR:** **엣지 과밀화 해소**: 하단의 `END` 노드 바로 위쪽에 `Merge / Sync`라는 숨겨진 가상 노드(버스터미널)를 도입했습니다.

- **EN:** **Clean Execution Flow**: All final outputs (like multiple `YES` paths from validation) now merge into a horizontal "Bus Line" before flowing down into the `END` node. This completely resolves the "logic black hole" where dozens of lines overlapped indistinguishably at the bottom of the screen.
- **KR:** **깔끔한 실행 흐름**: 유효성 검사에서 통과한 수많은 `YES` 경로들이 더 이상 한 점으로 무작정 모이지 않고, 수평선 형태의 굵은 버스(Bus) 라인 라우팅에 먼저 합류한 뒤 하나의 선으로 모여 `END`로 진입합니다. "논리적 블랙홀" 현상이 완전히 근절되었습니다.

### 4. Decision Node Resizing (결정 노드 비율 보정)
- **EN:** **Balanced Proportions**: Shrunk the overly bloated Diamond (Decision) nodes, preventing them from bleeding into sibling nodes horizontally and ensuring consistent grid spacing.
- **KR:** **균형 잡힌 비율**: 마름모꼴 결정(Decision) 노드들의 좌우폭이 너무 비대하여 형제 노드 영역을 침범하던 문제를 해결하기 위해 노드 너비를 압축(180px)하여, 캔버스의 일정한 간격 밸런스를 확보했습니다.

---

## 💡 Architectural Impact (설계적 가치)
- **EN:** With v0.2.14fix, the Flow View finally achieves the goal of being a **Tool for Engineers**. Logic execution now visually flows top-down with strict parent-child ranks, while orthogonal routing ensures intersections are readable and logic blocks are respected.
- **KR:** v0.2.14fix를 기점으로 SYNAPSE의 Flow 뷰는 진정한 **엔지니어를 위한 설계 도구**로서의 모습을 갖추었습니다. 논리의 인과 관계는 일관된 하강 계층(Rank)에 따라 배치되며, 직교 라우팅과 그룹 모듈화를 통해 7,000만 원짜리 아키텍트의 관제탑에 걸맞은 수준 높은 가독성을 제공합니다.
