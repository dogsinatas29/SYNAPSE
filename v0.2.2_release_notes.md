# 🚀 SYNAPSE v0.2.2 Release Notes

## 🇺🇸 English

### [New Features]
*   **📚 Documentation Shelf**: Markdown (`.md`) files are now automatically detected and placed in a dedicated "Documentation Shelf" area on the left side of the canvas. This keeps your architecture diagram clean while keeping documentation accessible.
*   **🎯 Manual Clustering & Grouping**: You can now manually select multiple nodes and group them into a cluster (Right-click or use toolbar). These manual clusters are now correctly preserved and reflected in the Mermaid export as `subgraph` blocks.
*   **🛠️ Refined File Scanning**: The extension now intelligently filters out non-source files (e.g., config files, build artifacts) and only focuses on programmable source code (ts, js, py, cpp, etc.) and documentation, reducing noise in large projects.

### [Bug Fixes]
*   **Fixed Manual Cluster Persistence**: Resolved an issue where manually created clusters would lose their visual grouping or fail to appear in exports due to a data transparency mismatch.
*   **Mermaid Export Reliability**: Added failsafes to ensure all UI-created groups are accurately generated in the Mermaid diagram code.

---

## 🇰🇷 한국어

### [신규 기능]
*   **📚 문서 선반 (Documentation Shelf)**: Markdown (`.md`) 파일들이 자동으로 감지되어 캔버스 좌측의 전용 "문서 선반" 영역에 배치됩니다. 이제 아키텍처 다이어그램을 어지럽히지 않으면서도 문서를 쉽게 참조할 수 있습니다.
*   **🎯 수동 클러스터링 및 그룹화**: 여러 노드를 선택하여 수동으로 그룹(클러스터)을 만들 수 있습니다 (우클릭 또는 툴바 사용). 이렇게 생성된 수동 클러스터는 Mermaid 내보내기 시에도 `subgraph`로 완벽하게 반영됩니다.
*   **🛠️ 파일 스캔 정교화**: 빌드 아티팩트나 설정 파일 등 비소스 파일을 똑똑하게 필터링하고, 프로그래밍 소스 코드(ts, js, py, cpp 등)와 문서 파일에만 집중하여 대규모 프로젝트에서도 깔끔한 다이어그램을 제공합니다.

### [버그 수정]
*   **수동 클러스터 지속성 수정**: 수동으로 생성한 클러스터가 시각적으로 깨지거나 내보내기에서 누락되던 데이터 불일치 문제를 해결했습니다.
*   **Mermaid 내보내기 신뢰성**: UI에서 생성된 모든 그룹이 Mermaid 다이어그램 코드에 정확히 생성되도록 안전장치를 추가했습니다.
